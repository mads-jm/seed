<?xml version="1.0" encoding="UTF-8"?><rss xmlns:dc="http://purl.org/dc/elements/1.1/" xmlns:content="http://purl.org/rss/1.0/modules/content/" xmlns:atom="http://www.w3.org/2005/Atom" version="2.0"><channel><title><![CDATA[{{SITE_NAME}}]]></title><description><![CDATA[Obsidian digital garden]]></description><link>http://github.com/dylang/node-rss</link><image><url>site-lib/media/favicon.png</url><title>{{SITE_NAME}}</title><link></link></image><generator>Webpage HTML Export plugin for Obsidian</generator><lastBuildDate>Wed, 29 Apr 2026 07:53:44 GMT</lastBuildDate><atom:link href="site-lib/rss.xml" rel="self" type="application/rss+xml"/><pubDate>Wed, 29 Apr 2026 07:53:21 GMT</pubDate><ttl>60</ttl><dc:creator></dc:creator><item><title><![CDATA[prestige-focus]]></title><description><![CDATA[
Status: fully implemented. Data model and event folds landed in Wave 6 (<a data-href="build-log-06-xp-calibration" href="05-notes/build-log-06-xp-calibration.html" class="internal-link" target="_self" rel="noopener nofollow">build-log-06-xp-calibration</a>); UI surface (token chip, focus arrows, phase-chooser modal, toasts) shipped in Wave 7.
<br>The second prestige system, parallel to <a data-href="prestige-integrate" href="08-specs/prestige-integrate.html" class="internal-link" target="_self" rel="noopener nofollow">prestige-integrate</a>. A tokens currency awarded every +99 cumulative trait levels gained. Spending a token activates a "phase" in which the user allocates a 4× bonus budget across one or more traits.<br>Integrate is identity and visual reward — slow, lifetime-scoped, cosmetic. Focus is the opposite: tactical, mid-term, expressive in the numbers. It lets the user accelerate progress on whatever feels most important right now (e.g. lean into reflection traits during a hard week) without inflating the long-run pacing contract from <a data-href="xp-pacing" href="08-specs/xp-pacing.html" class="internal-link" target="_self" rel="noopener nofollow">xp-pacing</a>.The two systems compose cleanly: integrate stacks visual layers, focus multiplies XP. They're independent enough that future tuning of one shouldn't disturb the other.A token is awarded on every 99 cumulative trait levels gained over the companion's lifetime.cumulative_levels_gained: u32 // sum of every level-up event ever observed
tokens_spent: u32
tokens_available() = (cumulative_levels_gained / 99) - tokens_spent
cumulative_levels_gained is monotonic non-decreasing. Integrate (which resets a trait's level to 1) does NOT subtract from it. The achievement of having reached those levels persists; the regrind counts forward, not backward.Prestige survives Reset. Action::Reset wipes per-trait XP and reminder runtime state but preserves all focus prestige state: cumulative_levels_gained, tokens_spent, active_focus, trait_integrations, and trait_enhancements. This is a load-bearing guarantee — prestige is a lifetime record of the user's investment, not a current-snapshot value. Resetting XP to start over does not erase that history.Spending a token opens a "phase" in which the user picks one allocation pattern:Patterns are not arithmetically equal — concentrate trades total power for peak XP rate; spread trades peak rate for breadth. This asymmetry is the design surface (the choice has weight). If the design later wants equal totals, swap Concentrate1x4 for Concentrate1x6 (one constant in the multiplier table).Only one focus configuration is active at a time. Spending a new token replaces the prior allocation. There is no "save &amp; continue" — phases end only by being replaced. There are no draft / preview states.Applied at XP-award time in xp_reward:fn xp_reward(reminder: &amp;Reminder, opts: XpRewardOpts, focus: Option&lt;&amp;FocusPhase&gt;) -&gt; u32
If focus has an allocation for the reminder's trait, multiply the result by 2 / 3 / 4 based on the arrow count for that trait. No focus, no multiplier. Initial state has active_focus: None, so behavior is unchanged until the user first spends a token.<br>Focus deliberately breaks the <a data-href="xp-pacing" href="08-specs/xp-pacing.html" class="internal-link" target="_self" rel="noopener nofollow">xp-pacing</a> contract for the duration of an active phase, but only on the allocated traits. The pacing band test in crates/seed-core/tests/levels.rs runs with focus: None, so the unboosted contract is what's enforced. A second test asserts that a Spread3x2 allocation across 3 traits yields exactly 2× their daily-XP budget — locking the multiplier semantics.struct State { // ...existing per-trait state, etc. cumulative_levels_gained: u32, tokens_spent: u32, active_focus: Option&lt;FocusPhase&gt;,
} struct FocusPhase { pattern: FocusPattern, allocations: Vec&lt;(TraitId, u8)&gt;, // (trait, arrow_count)
} enum FocusPattern { Concentrate1x4, Spread2x3, Spread3x2 }
The u8 in allocations is the arrow count (1, 2, or 3), which maps to multiplier (2×, 3×, 4×) via arrow_to_multiplier(). Storing the arrow count keeps the rendering code simple (just count the arrows to display) and the multiplier derivation explicit.<br>Event::FocusTokenEarned { new_balance } — kind: "seed.focus.token_earned". Emitted from inside the apply_event fold for LevelUp whenever the increment to cumulative_levels_gained crosses a 99-multiple boundary. Computed deterministically so replays produce identical token totals — the <a data-href="event-sourcing" href=".html" class="internal-link" target="_self" rel="noopener nofollow">event-sourcing</a> + <a data-href="snapshot-and-replay" href=".html" class="internal-link" target="_self" rel="noopener nofollow">snapshot-and-replay</a> invariants are load-bearing here.Event::FocusPhaseActivated { pattern, traits } — kind: "seed.focus.phase_activated". traits: Vec&lt;TraitId&gt; has exactly 1, 2, or 3 entries depending on pattern. apply_event:
Validate traits.len() matches the pattern (1 / 2 / 3 respectively); reject otherwise.
Validate tokens_available() &gt; 0; reject otherwise.
Increment tokens_spent.
Replace active_focus with a new FocusPhase populated from the event.
There is no Event::FocusPhaseEnded — phases end only by being replaced.<br>Document both variants in <a data-href="events-schema" href="02-references/events-schema.html" class="internal-link" target="_self" rel="noopener nofollow">events-schema</a>. Both are additive (<a data-href="wire-versioning" href=".html" class="internal-link" target="_self" rel="noopener nofollow">wire-versioning</a>: new kinds, no v bump) so older clients tolerate them via Event::Unknown round-trip.fn tokens_available(state: &amp;State) -&gt; u32 { state.cumulative_levels_gained / 99 - state.tokens_spent
} fn can_activate_phase(state: &amp;State) -&gt; bool { tokens_available(state) &gt; 0
} fn arrow_to_multiplier(arrows: u8) -&gt; u32 { match arrows { 1 =&gt; 2, 2 =&gt; 3, 3 =&gt; 4, _ =&gt; 1 }
} This spec.
State additions in crates/seed-core/src/state.rs.
FocusPattern enum + FocusPhase struct in crates/seed-core/src/domain.rs.
Update xp_reward signature to accept Option&lt;&amp;FocusPhase&gt;; apply multiplier when allocation matches.
Event::FocusTokenEarned, Event::FocusPhaseActivated variants + folds in crates/seed-core/src/events.rs. Token-earning logic in the LevelUp fold.
Update daemon's apply_event for ReminderCompleted to read state.active_focus and pass it to xp_reward.
<br>Document events in <a data-href="events-schema" href="02-references/events-schema.html" class="internal-link" target="_self" rel="noopener nofollow">events-schema</a>.
Round-trip + fold tests in crates/seed-core/tests/events.rs. Replay determinism test for token-earning. Multiplier test in crates/seed-core/tests/levels.rs.
Shipped in Wave 7:
Token-balance chip (★N) in the title bar, shown only when tokens_available &gt; 0.
Focus-token toast (+1 focus token (N total)) fired via StateDiff when FocusTokenEarned arrives.
Active focus arrows (▲×N) on LEVELS rows for traits with a current focus allocation.
f key — when command bar is empty and tokens &gt; 0, opens the phase-chooser modal.
Phase-chooser modal — two-stage: Step 1 selects allocation pattern (Spread 3x2 / Spread 2x3 / Concentrate), Step 2 toggles traits (enforces arity per pattern), Enter dispatches Action::ActivateFocusPhase, Esc backs up or cancels.
/focus &lt;pattern&gt; &lt;trait1&gt; [trait2] [trait3] slash command — dispatches directly; validates pattern name and trait arity. Persistent focus history (timeline of past phases). Easy add later — events are already in the log.
<br>Interaction with <a data-href="prestige-integrate" href="08-specs/prestige-integrate.html" class="internal-link" target="_self" rel="noopener nofollow">prestige-integrate</a> — they're independent; no combo bonus or cross-system effect. The three patterns are not arithmetically equal (raw multiplier sums are 4 / 6 / 6). Read as deliberate concentrate-vs-spread tradeoff. If parity is desired, Concentrate1x6 is a one-line change.
A user could in principle hold many tokens and never spend them. That's fine — tokens persist. No expiry.
Token-earning fires inside LevelUp fold, which means replaying an old event log produces the same token total deterministically. Care needed if cumulative_levels_gained initialization ever changes (e.g. retro-active count from existing snapshots) — bump events v in that case. <br><a data-href="xp-pacing" href="08-specs/xp-pacing.html" class="internal-link" target="_self" rel="noopener nofollow">xp-pacing</a> — the unboosted contract focus multiplies against
<br><a data-href="prestige-integrate" href="08-specs/prestige-integrate.html" class="internal-link" target="_self" rel="noopener nofollow">prestige-integrate</a> — the visual / cosmetic prestige system
<br><a data-href="event-sourcing" href=".html" class="internal-link" target="_self" rel="noopener nofollow">event-sourcing</a> — why token earning is part of the LevelUp fold rather than daemon state
<br><a data-href="snapshot-and-replay" href=".html" class="internal-link" target="_self" rel="noopener nofollow">snapshot-and-replay</a> — what makes token-total replay determinism load-bearing
<br><a data-href="cli-flags" href="02-references/cli-flags.html" class="internal-link" target="_self" rel="noopener nofollow">cli-flags</a> — --dev mode is needed to manually emit focus events for testing
]]></description><link>08-specs/prestige-focus.html</link><guid isPermaLink="false">08 specs/prestige-focus.md</guid><pubDate>Wed, 29 Apr 2026 07:52:21 GMT</pubDate></item><item><title><![CDATA[prestige-integrate]]></title><description><![CDATA[
Status: fully implemented. Data model landed in Wave 6 (<a data-href="build-log-06-xp-calibration" href="05-notes/build-log-06-xp-calibration.html" class="internal-link" target="_self" rel="noopener nofollow">build-log-06-xp-calibration</a>); UI surface (enhancement-chooser modal, [I] affordance, integration count badges) shipped in Wave 7.
The first of two prestige systems. Per-trait, voluntary, cosmetic-only. When a trait reaches lvl 99 the user may "integrate" it: the trait resets to 1 and the user picks a visual enhancement that persists on the glyph forever. Each subsequent integration of the same trait stacks another enhancement linearly.<br>The 1-year time-to-99 contract from <a data-href="xp-pacing" href="08-specs/xp-pacing.html" class="internal-link" target="_self" rel="noopener nofollow">xp-pacing</a> makes lvl 99 a real achievement but not the end of the story. Integrate is the ritual that turns the achievement into something visible on the companion. It also keeps the gameplay loop alive past 99 without inflating XP rates — the reward is identity and visual depth, not faster numbers.Available only when the trait's level is exactly 99 (XP ≥ xp_for_level(99)). The user explicitly invokes integrate; nothing happens automatically at 99.
Trait XP resets to 0 (level → 1).
The trait's integrations: u8 counter increments.
The user's chosen enhancement (IntegrationEnhancement) is appended to that trait's enhancements: Vec&lt;IntegrationEnhancement&gt; list. Order is preserved; enhancements never replace each other. No XP-rate change. Linear scaling means the visual effect stacks; XP/hr stays at the contract value. Pacing is preserved across integrations. If a future XP buff is wanted, model it as a separate prestige_xp_mult field — don't smuggle it into integrate.
<br>No tier-table change. <a data-href="tier-progression" href=".html" class="internal-link" target="_self" rel="noopener nofollow">tier-progression</a> is computed from total level across all 9 traits. Integrating one trait reduces total level by 98, so tier may step down — that's intentional. Tier is the snapshot; integrate is the lifetime achievement (tracked separately in the integrations counter and the persistent enhancements).
No cross-trait coupling. Each trait's integrations are independent. Integrating flow does not affect core.
struct TraitState { // ...existing xp, last_done_ms, etc. integrations: u8, // count of times integrated enhancements: Vec&lt;IntegrationEnhancement&gt;, // chosen visual enhancements, append-only
}
u8 is sufficient — even at one integration per year over a lifetime you never overflow.A starter set is one cosmetic per trait (e.g. FlowSpiral, CoreEmber, SpineLattice, ReachBranch, ClarityRing, SpaceVeil, DepthAbyss, ResonanceChord, WarmthGlow). Expandable; serialized by stable string id so the catalog can grow without breaking persisted state.Selection happens at integration time. The user-facing UI (deferred) presents available enhancements; this spec only commits to the data model.{ "v": 1, "ts": "2026-05-01T12:00:00Z", "kind": "seed.trait.integrated", "data": { "trait_id": "flow", "new_integrations": 1, "enhancement_id": "FlowSpiral" }
}
<br>apply_event for TraitIntegrated (<a data-href="event-sourcing" href=".html" class="internal-link" target="_self" rel="noopener nofollow">event-sourcing</a> fold; lives in <a data-href="pure-core" href=".html" class="internal-link" target="_self" rel="noopener nofollow">pure-core</a>):
Validate trait is at lvl 99 (else ignore — defensive against malformed event injection).
Set xp = 0.
Increment integrations.
Push enhancement_id onto enhancements.
<br>Document this variant in <a data-href="events-schema" href="02-references/events-schema.html" class="internal-link" target="_self" rel="noopener nofollow">events-schema</a>. Adding it is non-breaking (<a data-href="wire-versioning" href=".html" class="internal-link" target="_self" rel="noopener nofollow">wire-versioning</a>: new kind, no v bump).Out of scope for the data-model pass. When rendering ships:
<br>Each enhancement maps to a renderer hook that draws additional layers / modifies char distribution / shifts color blend for that trait's contribution to the glyph. See <a data-href="glyph-layer-composition" href=".html" class="internal-link" target="_self" rel="noopener nofollow">glyph-layer-composition</a>.
Multiple enhancements on the same trait stack additively (linear scaling — three FlowSpirals are three nested spirals, not one larger spiral).
<br>Renderer remains pure / deterministic from (traits, enhancements, seed) — same <a data-href="pure-core" href=".html" class="internal-link" target="_self" rel="noopener nofollow">pure-core</a> invariant as the rest of the rendering pipeline. This spec.
State additions in crates/seed-core/src/state.rs.
IntegrationEnhancement enum in crates/seed-core/src/domain.rs.
Event::TraitIntegrated variant + fold in crates/seed-core/src/events.rs.
<br>Document the event in <a data-href="events-schema" href="02-references/events-schema.html" class="internal-link" target="_self" rel="noopener nofollow">events-schema</a>.
Round-trip + fold tests in crates/seed-core/tests/events.rs — confirm xp resets, integrations increments, enhancement persists across snapshot save/load.
Shipped in Wave 7:
[I] affordance on LEVELS rows when trait XP ≥ xp_for_level(99) — signals integrate is available.
Integration count badge (✦N) on LEVELS rows and in the skill-detail overlay when N &gt; 0.
Enhancement-chooser modal — opens on Enter at a level-99 LEVELS row (or via /integrate &lt;trait&gt;). Shows prior integration count, one enhancement option per trait (starter set), Enter to confirm, Esc to cancel.
/integrate &lt;trait&gt; [enhancement] slash command — dispatches Action::Integrate directly; enhancement defaults to the per-trait starter if omitted. Glyph rendering of enhancements.
Enhancement catalog beyond the per-trait starter set.
Cross-trait combo enhancements ("when both flow and space are integrated, unlock X") — tempting but out of scope until the base system has lived experience behind it. <br><a data-href="xp-pacing" href="08-specs/xp-pacing.html" class="internal-link" target="_self" rel="noopener nofollow">xp-pacing</a> — the contract integrate must preserve
<br><a data-href="prestige-focus" href="08-specs/prestige-focus.html" class="internal-link" target="_self" rel="noopener nofollow">prestige-focus</a> — the parallel prestige system (currency-driven, XP-multiplier-based)
<br><a data-href="event-sourcing" href=".html" class="internal-link" target="_self" rel="noopener nofollow">event-sourcing</a> — why this is just one more event variant against the existing fold
<br><a data-href="glyph-layer-composition" href=".html" class="internal-link" target="_self" rel="noopener nofollow">glyph-layer-composition</a> — the substrate enhancements stack on
<br><a data-href="tier-progression" href=".html" class="internal-link" target="_self" rel="noopener nofollow">tier-progression</a> — why integrating one trait can step the companion's tier down
]]></description><link>08-specs/prestige-integrate.html</link><guid isPermaLink="false">08 specs/prestige-integrate.md</guid><pubDate>Wed, 29 Apr 2026 07:52:17 GMT</pubDate></item><item><title><![CDATA[index]]></title><description><![CDATA[
For LLMs: Start here. Navigate to a directory index for scoped exploration, or jump directly to a core document below. <a data-href="CLAUDE" href="claude.html" class="internal-link" target="_self" rel="noopener nofollow">CLAUDE</a> For Humans: See above… but use the graph to guide your exploration!
.obsidian/ is vault configuration and snippet state, not part of the documentation corpus. docs/ at the project root is the GitHub Pages export target — it is build output, not source.
<br><a data-href="README" href="readme.html" class="internal-link" target="_self" rel="noopener nofollow">README</a>
<br><a data-href="v0-mvp" href="09-milestones/v0-mvp.html" class="internal-link" target="_self" rel="noopener nofollow">v0-mvp</a> — historical record of the v0 MVP build (locked decisions, TASK-001..012, prestige pre-wiring).
<br><a data-href="v0-1-0-punch-list" href="08-specs/v0-1-0-punch-list.html" class="internal-link" target="_self" rel="noopener nofollow">v0-1-0-punch-list</a> — full prose for the active backlog (TASK-013..028, cross-cutting threads, suggested cut order).
<br><a data-href="BACKLOG.kanban" href="00-index/backlog.kanban.html" class="internal-link" target="_self" rel="noopener nofollow">BACKLOG.kanban</a> — active board, one card per active task.
<br><a data-href="events-schema" href="02-references/events-schema.html" class="internal-link" target="_self" rel="noopener nofollow">events-schema</a> — wire-protocol contract.
<br><a data-href="cargo-cheatsheet" href="03-guides/cargo-cheatsheet.html" class="internal-link" target="_self" rel="noopener nofollow">cargo-cheatsheet</a> — cargo workflow reference.
<br><a data-href="ARCHITECTURE" href="04-architecture/architecture.html" class="internal-link" target="_self" rel="noopener nofollow">ARCHITECTURE</a> — workspace layout, crate boundaries, event/IPC design.
<br><a data-href="ADR" href="04-architecture/adr/adr.html" class="internal-link" target="_self" rel="noopener nofollow">ADR</a> — architectural decision records.
<br><a data-href="SPECS" href="08-specs/specs.html" class="internal-link" target="_self" rel="noopener nofollow">SPECS</a> — feature and component specifications index.
<br><a data-href="CONCEPTS" href="01-concepts/concepts.html" class="internal-link" target="_self" rel="noopener nofollow">CONCEPTS</a> — durable patterns referenced across the vault. The eight planned notes are listed there as unresolved wikilinks until written.
cargo build # build the workspace
cargo test # run all tests
cargo run -p seed-tui # launch the TUI (auto-spawns seedd if absent)
cargo run -p seed-daemon -- --foreground # run the daemon in foreground
SEED_LOG=debug cargo run -p seed-tui # verbose logging
Last Updated: 2026-04-29
Documentation Version: v0.1.0]]></description><link>index.html</link><guid isPermaLink="false">index.md</guid><pubDate>Wed, 29 Apr 2026 07:50:45 GMT</pubDate></item><item><title><![CDATA[build-log-06b-redline-fixes]]></title><description><![CDATA[Date: 2026-04-27 · Wave: 6 · Predecessor: <a data-href="build-log-06-xp-calibration" href="05-notes/build-log-06-xp-calibration.html" class="internal-link" target="_self" rel="noopener nofollow">build-log-06-xp-calibration</a><br>All redline findings from the inspector review of Wave 06 (XP calibration + prestige pre-wiring). The blocker fix (C1) was a load-bearing <a data-href="event-sourcing" href=".html" class="internal-link" target="_self" rel="noopener nofollow">event-sourcing</a> correctness bug — cumulative_levels_gained literally never incremented in production, so tokens never accrued. Tests + clippy + fmt clean post-fixes; binaries linked from cargo check (real .exes were locked by running processes during the fix-pass).LevelUp fold computed old_level = level_for_xp(state.traits[trait_id]), but ReminderCompleted ran first in the same commit batch and had already written new_xp into state.traits. By the time LevelUp folded, level_for_xp(*xp) == new_level, so levels_gained = 0. Tokens never accrued.<br>Fix: Added old_level: u8 to the LevelUp event payload. The fold now uses the payload, never reads pre-event state. The daemon's build_complete_events computes old_level = level_for_xp(current_xp) before building events, when pre-completion XP is still in hand. #[serde(default)] keeps old log entries deserializing cleanly (they fall back to effective_old = new_level - 1 — safe undercount). No version bump (additive field per <a data-href="wire-versioning" href=".html" class="internal-link" target="_self" rel="noopener nofollow">wire-versioning</a>).build_complete_events hardcoded XpRewardOpts { on_time: true, overdue: false }. Every real completion since the daemon shipped paid the 1.0 (on-time) rate regardless of state. Users completing overdue reminders were underrewarded (1.0 vs 1.4); users completing early were overrewarded (1.0 vs 0.6).Fix: Compute XpRewardOpts from reminder_status_with_interval(rt.interval_min, rt.last_done_ms, rt.enabled, now_ms) using pre-completion last_done_ms. Overdue → overdue: true; Due → on_time: true; Dormant/Off (early completion) → both false (0.6× late penalty). Validated via the --ignored smoke test; a unit test against build_complete_events directly is impractical without removing the &amp;self bound.*count = *new_integrations trusted the payload verbatim. A malformed event could rewrite the integration count.Fix: *count = count.saturating_add(1). The new_integrations field is retained as audit trail; the fold ignores it for the actual mutation. #[cfg(debug_assertions)] eprintln! on mismatch flags drift without a tracing dep. New test: integrate_twice_increments_to_two.
tests/events.rs:380: get(…).is_none() → !contains_key(…).
view/tweaks.rs (6 instances): Default::default() + field reassignments → struct literal with ..Default::default().
User direction: "variance being wide is okay; just define a broader contract and ensure it makes sense with the difficulty / friction of a skill."<br>Fix: Replaced the single [3000, 6000]-for-all approach with per-trait bands derived from each trait's fire profile. The test pacing_band_per_trait drives a (trait, lo, hi) table. clarity and space get the widest ([3000, 6000]) because their 4-hour anchors (sun, rest) are structurally incompatible with a tight daily band while still being within contract at the per-reminder XP/hr level. A 15% cadence drift in any reminder would fail even the widest band. Spec table updated in <a data-href="xp-pacing" href="08-specs/xp-pacing.html" class="internal-link" target="_self" rel="noopener nofollow">xp-pacing</a>.User direction: "Think of prestige as its own gain; we'll eventually tune back non-prestige visuals and have permanent visual gain on prestige."<br>Fix: Action::Reset snapshots prestige state (cumulative_levels_gained, tokens_spent, active_focus, trait_integrations, trait_enhancements) before building initial_state, then carries them forward. Per-trait XP and reminder runtimes are reset. Load-bearing guarantee documented in <a data-href="prestige-focus" href="08-specs/prestige-focus.html" class="internal-link" target="_self" rel="noopener nofollow">prestige-focus</a>.Reverted: actual ratio is 0.5007, well within 0.01. Tightened back in both levels.rs (inline test) and tests/levels.rs (external).Fix: Duplicate-detection loop in the fold using a BTreeSet. Rejects and logs on first duplicate; token is not spent. New test.Fix: Existence check after the duplicate check, matching the defensive pattern from ReminderCompleted. New test.self.state.read().await was being held twice — once for the main fold, once to compute new_balance for FocusTokenEarned. Consolidated into a single read that captures current_xp, rt, xp_opts, cumulative_before, tokens_spent, active_focus. Doc comment names the single-writer invariant at cmd_rx.recv().
C1 fix via payload field, not fold restructure — adding old_level to LevelUp keeps the fold pure (&amp;mut State → ()) and the wire format extensible. Restructuring to return Vec&lt;Event&gt; from the fold would have been a much bigger surface change.
M6 prestige preservation copies by value before write — a concurrent snapshot between the read and write would see pre-reset state. Acceptable given the daemon is single-writer at cmd_rx.recv(). <br><a data-href="build-log-06-xp-calibration" href="05-notes/build-log-06-xp-calibration.html" class="internal-link" target="_self" rel="noopener nofollow">build-log-06-xp-calibration</a> — the wave this corrects.
<br><a data-href="v0-mvp" href="09-milestones/v0-mvp.html" class="internal-link" target="_self" rel="noopener nofollow">v0-mvp</a> — consolidated milestone.
<br><a data-href="v0-1-0-punch-list" href="08-specs/v0-1-0-punch-list.html" class="internal-link" target="_self" rel="noopener nofollow">v0-1-0-punch-list</a> — successor backlog (the eprintln! from C3/m9 is in the <a data-tooltip-position="top" aria-label="v0-1-0-punch-list > Minor / nit cleanup batch" data-href="v0-1-0-punch-list#Minor / nit cleanup batch" href="08-specs/v0-1-0-punch-list.html#Minor / nit cleanup batch" class="internal-link" target="_self" rel="noopener nofollow">nit batch</a>).
<br><a data-href="event-sourcing" href=".html" class="internal-link" target="_self" rel="noopener nofollow">event-sourcing</a>, <a data-href="wire-versioning" href=".html" class="internal-link" target="_self" rel="noopener nofollow">wire-versioning</a> — invariants C1 and C2 restore.
]]></description><link>05-notes/build-log-06b-redline-fixes.html</link><guid isPermaLink="false">05 notes/build-log-06b-redline-fixes.md</guid><pubDate>Wed, 29 Apr 2026 07:50:29 GMT</pubDate></item><item><title><![CDATA[build-log-06-xp-calibration]]></title><description><![CDATA[Date: 2026-04-27 · Wave: 6 · Specs: <a data-href="xp-pacing" href="08-specs/xp-pacing.html" class="internal-link" target="_self" rel="noopener nofollow">xp-pacing</a> · <a data-href="prestige-integrate" href="08-specs/prestige-integrate.html" class="internal-link" target="_self" rel="noopener nofollow">prestige-integrate</a> · <a data-href="prestige-focus" href="08-specs/prestige-focus.html" class="internal-link" target="_self" rel="noopener nofollow">prestige-focus</a>Reshape the XP economy to enforce the 1-year-to-99 contract uniformly across all 9 traits, and pre-wire the two prestige systems' data models without UI surface. Eight stages, all shipped.After this wave: 163/163 tests in seed-core; 36/36 in seed-tui lib. Clippy + fmt clean.
<br>
LevelUp fold can't return derivative events — apply_event is &amp;mut State → () (the <a data-href="event-sourcing" href=".html" class="internal-link" target="_self" rel="noopener nofollow">event-sourcing</a> / <a data-href="pure-core" href=".html" class="internal-link" target="_self" rel="noopener nofollow">pure-core</a> contract). We need to emit FocusTokenEarned deterministically when cumulative_levels_gained crosses a 99-multiple. Solution: the fold mutates cumulative_levels_gained (deterministic on replay); the daemon's build_complete_events detects the boundary crossing pre/post-fold and emits the wire signal. FocusTokenEarned's own fold is a no-op — it's a UI signal, like CompanionAwakened and TierChanged. The <a data-href="snapshot-and-replay" href=".html" class="internal-link" target="_self" rel="noopener nofollow">snapshot-and-replay</a> invariant holds because the cumulative state is what carries forward, not the signal event. xp_reward composition order — xp_per_completion × time_mult × focus_mult, left to right, fp throughout, rounded at the end. The on-time base is canonical; both multipliers fan around it. Documented in the function comment. Pacing band wide ceiling for clarity / space / reach — one ultra-frequent reminder (look 20 min, breathe 25 min) paired with one anchor reminder (sun / rest 240 min) sums above 4200/day; the per-reminder XP/hr is within contract, only the trait sum is high. reach compounds three moderate-cadence reminders. Each individually on-band. The wide ceiling (6000) is not slack — a 15% drift falls outside. depth fire-rate calculation — reminders with interval &gt; active window (journal, reflect at 1440 min vs. 900 min active) fire at 1/day, not 900/1440 = 0.625/day. The pacing test now branches on interval &gt; active_window. OSRS ratio epsilon widened to 0.02 — floor division after SCALE_DIVISOR = 10 accumulates rounding. The actual ratio is 0.5007 (well within 0.01), but the spec asked for 0.02 with a comment. <br>
No tracing in seed-core — the fold needs to validate inputs but seed-core has no tracing dep (<a data-href="pure-core" href=".html" class="internal-link" target="_self" rel="noopener nofollow">pure-core</a>). Used #[cfg(debug_assertions)] eprintln! instead. Tracked in <a data-tooltip-position="top" aria-label="v0-1-0-punch-list > Minor / nit cleanup batch" data-href="v0-1-0-punch-list#Minor / nit cleanup batch" href="08-specs/v0-1-0-punch-list.html#Minor / nit cleanup batch" class="internal-link" target="_self" rel="noopener nofollow">the nit batch</a> — eprintln! from a fold breaks raw-mode TUI rendering and should move to the daemon. Integrate UI: enhancement chooser at lvl 99.
Focus UI: token balance display, phase chooser, arrow rendering on LEVELS.
Glyph rendering of enhancements (each enhancement adds a layer to the trait's contribution).
<br>A minimum-viable hint surface in v0.1.0 — token counter on LEVELS or "INTEGRATE READY" status — is tracked as TASK-024 in <a data-href="v0-1-0-punch-list" href="08-specs/v0-1-0-punch-list.html" class="internal-link" target="_self" rel="noopener nofollow">v0-1-0-punch-list</a>. <br><a data-href="build-log-06b-redline-fixes" href="05-notes/build-log-06b-redline-fixes.html" class="internal-link" target="_self" rel="noopener nofollow">build-log-06b-redline-fixes</a> — inspector findings on this wave + corrective patches.
<br><a data-href="xp-pacing" href="08-specs/xp-pacing.html" class="internal-link" target="_self" rel="noopener nofollow">xp-pacing</a>, <a data-href="prestige-integrate" href="08-specs/prestige-integrate.html" class="internal-link" target="_self" rel="noopener nofollow">prestige-integrate</a>, <a data-href="prestige-focus" href="08-specs/prestige-focus.html" class="internal-link" target="_self" rel="noopener nofollow">prestige-focus</a> — the specs.
<br><a data-href="v0-mvp" href="09-milestones/v0-mvp.html" class="internal-link" target="_self" rel="noopener nofollow">v0-mvp</a> — consolidated milestone.
<br><a data-href="event-sourcing" href=".html" class="internal-link" target="_self" rel="noopener nofollow">event-sourcing</a>, <a data-href="pure-core" href=".html" class="internal-link" target="_self" rel="noopener nofollow">pure-core</a>, <a data-href="snapshot-and-replay" href=".html" class="internal-link" target="_self" rel="noopener nofollow">snapshot-and-replay</a> — the invariants the prestige pre-wiring leans on.
]]></description><link>05-notes/build-log-06-xp-calibration.html</link><guid isPermaLink="false">05 notes/build-log-06-xp-calibration.md</guid><pubDate>Wed, 29 Apr 2026 07:50:07 GMT</pubDate></item><item><title><![CDATA[build-log-05-docs-and-smoke]]></title><description><![CDATA[Tasks: TASK-011 · Wave: 5README, the wire-schema doc, and the end-to-end smoke test that ties the whole thing together. This wave was the v0 ship gate — inspector recommended SHIP after this pass. The smoke test exercises <a data-href="snapshot-and-replay" href=".html" class="internal-link" target="_self" rel="noopener nofollow">snapshot-and-replay</a> end-to-end (real seedd against a tempfile::TempDir).
README.md — full v0 README (why / install / quickstart / commands / config / troubleshooting / pour integration / tech stack / dev).
<br>events-schema.md — every Event variant documented with locked JSON examples derived from the snapshot tests. (Now lives at <a data-href="events-schema" href="02-references/events-schema.html" class="internal-link" target="_self" rel="noopener nofollow">events-schema</a> post-migration.)
crates/seed-daemon/src/lib.rs — new [lib] target exposing wire, ipc, event_log, daemon, notify, schedule. main.rs switched to importing through the lib.
crates/seed-daemon/tests/smoke.rs — end-to-end: spawns real seedd against a tempfile::TempDir, drives Subscribe → Complete → Shutdown, asserts events.jsonl contains the expected envelope. #[ignore] by default; runs explicitly with cargo test --test smoke -- --ignored. <br>Daemon → lib + bin — the smoke test needed seed_daemon::wire::* and seed_daemon::ipc::socket_name. Converting to a lib+bin pair was cleaner than duplicating the wire types a third time. (Note: the TUI's client.rs still has its own copy from Wave 4 — TASK-020 in <a data-href="v0-1-0-punch-list" href="08-specs/v0-1-0-punch-list.html" class="internal-link" target="_self" rel="noopener nofollow">v0-1-0-punch-list</a> closes that loop.)
Hello drain in the smoke probe — the daemon sends a Hello frame on every accept. The original probe sent Ping immediately and read Hello back, mismatching Pong and timing out at 5s even on a healthy daemon. Probe now reads-and-matches Hello first, then sends Ping.
Smoke is #[ignore] by default — running it requires the seedd binary built in the same target/ profile; cargo test --workspace doesn't guarantee that for integration tests. Explicit invocation surfaces in CI as a separate gate.
<br>The final-sweep inspector recommended SHIP v0 with nothing blocking. Findings carried forward into <a data-href="v0-1-0-punch-list" href="08-specs/v0-1-0-punch-list.html" class="internal-link" target="_self" rel="noopener nofollow">v0-1-0-punch-list</a>:Shipped: full Cargo workspace; 9 traits × 20 reminders; OSRS curve 1–99; SEED → ZENITH tier progression (10 tiers); event-sourced state with snapshot + tail-replay; 5 palettes with truecolor + 256 + ASCII fallbacks; tweaks panel with two-step reset; auto-reconnect with backoff; panic hook + TerminalGuard; rotating logs; seed init scaffold; smoke test; 218 unit/integration tests + 1 smoke (ignored).<br>Deferred to the v0.1.0 punch list and beyond: quit-flush, disconnect toast, Action::Subscribe rename, fractional XP-drain accumulator, time-compression in tweaks, prebuilt install scripts, click/mouse on orbit cards, pour integration. All carried in <a data-href="v0-1-0-punch-list" href="08-specs/v0-1-0-punch-list.html" class="internal-link" target="_self" rel="noopener nofollow">v0-1-0-punch-list</a> or in the post-Wave-6 prestige work.
<br><a data-href="build-log-06-xp-calibration" href="05-notes/build-log-06-xp-calibration.html" class="internal-link" target="_self" rel="noopener nofollow">build-log-06-xp-calibration</a> — Wave 6, post-ship recalibration of the XP economy + prestige pre-wiring.
<br><a data-href="v0-mvp" href="09-milestones/v0-mvp.html" class="internal-link" target="_self" rel="noopener nofollow">v0-mvp</a> — consolidated milestone these notes feed into.
<br><a data-href="snapshot-and-replay" href=".html" class="internal-link" target="_self" rel="noopener nofollow">snapshot-and-replay</a>, <a data-href="wire-versioning" href=".html" class="internal-link" target="_self" rel="noopener nofollow">wire-versioning</a> — invariants the smoke test and the events-schema doc depend on.
]]></description><link>05-notes/build-log-05-docs-and-smoke.html</link><guid isPermaLink="false">05 notes/build-log-05-docs-and-smoke.md</guid><pubDate>Wed, 29 Apr 2026 07:49:40 GMT</pubDate></item><item><title><![CDATA[build-log-04-seed-tui]]></title><description><![CDATA[Tasks: TASK-008 · TASK-009 · TASK-010 · Wave: 4The TUI: IPC client + state mirror (TASK-008), full ratatui layout (TASK-009), cross-cutting logging/panic/terminal shims (TASK-010). The TUI is a thin reader — it folds incoming StateDiff events through seed_core::apply_event to mirror state locally and never mutates (<a data-href="event-sourcing" href=".html" class="internal-link" target="_self" rel="noopener nofollow">event-sourcing</a>). The render path implements the <a data-href="terminal-capability-ladder" href=".html" class="internal-link" target="_self" rel="noopener nofollow">terminal-capability-ladder</a> (truecolor → 256 → ASCII; braille → block). Initial pass: 204/204 tests green; after Wave 4.1 fixes, 220/220.
<br>Wire types duplicated in client.rs — at v0, seed-daemon had no [lib] target; binary crates can't be imported. ~100 lines of pure serde structs duplicated rather than restructure the daemon for a couple of types. JSON compatibility is the contract. (Wave 5 added a daemon lib.rs; deduplicating is now TASK-020 in <a data-href="v0-1-0-punch-list" href="08-specs/v0-1-0-punch-list.html" class="internal-link" target="_self" rel="noopener nofollow">v0-1-0-punch-list</a>.)
<br>Terminal capabilities are env-driven, not detected — $COLORTERM in ("truecolor", "24bit") for truecolor; SEED_FORCE_256 and SEED_FORCE_ASCII as escape hatches. Querying terminal emulators for Unicode coverage is infeasible; documented in --help. See <a data-href="terminal-capability-ladder" href=".html" class="internal-link" target="_self" rel="noopener nofollow">terminal-capability-ladder</a>.
<br>Braille fallback is a TUI-side post-process — rather than a renderer overload in seed-core, braille_to_block() maps each braille char to a block-density equivalent based on popcount. Keeps the <a data-href="pure-core" href=".html" class="internal-link" target="_self" rel="noopener nofollow">pure-core</a> invariant intact.
Daemon auto-spawn on Windows uses DETACHED_PROCESS | CREATE_NO_WINDOW — without these flags the daemon shares the TUI's console and dies on CTRL_CLOSE_EVENT when the TUI exits. The daemon binary is located via current_exe().parent().
Shimmer is deterministic — hash of (position, tick/3) at ~6 Hz, gate 1/31 cells at intensity ≥ 4. Same (position, tick) always swaps the same way; no flicker, no double-buffer artifacts.
IpcClient uses a separate tokio task + channel handoff — all socket I/O in ipc_io_task, mpsc::Receiver&lt;Message&gt; to UI. The render loop's select! reads from client.inbound and never blocks on socket I/O.
Panic hook restores terminal before invoking the default hook — disable_raw_mode() + LeaveAlternateScreen + cursor show happen first, then the captured default hook runs. TerminalGuard::Drop provides a second restore path for normal exits. Both calls to disable_raw_mode() are idempotent.
The inspector pass cleared the critical path (gates green, happy path works) but flagged seven issues worth fixing before smoke-testing.
F2b · Quit-flush (TASK-009 AC: "Quit flushes pending events to daemon before exit") — should_quit breaks the loop and drops IpcClient before the I/O task drains. Fine in practice (capacity-64 mpsc, tiny bursts), but the AC is unmet.
<br>F3 · Silent disconnect feedback — when the I/O task is dead, user verbs are dropped with only a file-log warn!. No toast, no log pane entry. Tracked as part of TASK-026 in <a data-href="v0-1-0-punch-list" href="08-specs/v0-1-0-punch-list.html" class="internal-link" target="_self" rel="noopener nofollow">v0-1-0-punch-list</a>.
F5 · pick_orbit_reminders per-frame allocations — two Vec clones, BTreeMap clone, full GlyphFrame::clone() on non-truecolor terminals (~4000 cells). Acceptable at 20 Hz today; profile before scaling up.
F8 · Action::Subscribe is misnamed — it's really GetSnapshot; daemon auto-subscribes to broadcasts on accept. Rename is a breaking protocol change; deferred.
F11 · No daemon-stop affordance from the TUI on Windows — by design (DETACHED_PROCESS). README troubleshooting calls out taskkill.
F16 · 3s snapshot timeout on cold start — late-arriving Snapshot does overwrite, but the user sees stale initial_state briefly. Documented in README troubleshooting.
<br>F17 · #[allow(dead_code)] on App hides genuinely-unused fields — config, seed_home. Tracked in TASK-028 in <a data-href="v0-1-0-punch-list" href="08-specs/v0-1-0-punch-list.html" class="internal-link" target="_self" rel="noopener nofollow">v0-1-0-punch-list</a> (alongside the broader carving work).
Field-by-field diff of seed-daemon/src/wire.rs vs seed-tui/src/client.rs: zero drift. Message (8 variants), ResponseResult, Action (9 variants — 10 after Wave 4.1 added Reset), MAX_FRAME = 4 MiB, framing — all identical. The duplication is a maintenance burden but not a correctness risk; closing TASK-020 removes it entirely.
Click/mouse on orbit cards — keyboard-first; out of scope for v0.
Orbit ellipse dashes (the JSX has 3 SVG dashed ellipses) — produces noise at terminal cell granularity.
Time compression / tick-rate dev knob. <br><a data-href="build-log-05-docs-and-smoke" href="05-notes/build-log-05-docs-and-smoke.html" class="internal-link" target="_self" rel="noopener nofollow">build-log-05-docs-and-smoke</a> — README + smoke test (TASK-011).
<br><a data-href="v0-1-0-punch-list" href="08-specs/v0-1-0-punch-list.html" class="internal-link" target="_self" rel="noopener nofollow">v0-1-0-punch-list</a> — successor work (TASK-013 snooze, TASK-019 client_log, TASK-020 wire dedup, TASK-028 carve).
<br><a data-href="event-sourcing" href=".html" class="internal-link" target="_self" rel="noopener nofollow">event-sourcing</a>, <a data-href="terminal-capability-ladder" href=".html" class="internal-link" target="_self" rel="noopener nofollow">terminal-capability-ladder</a> — the invariants this wave depends on.
]]></description><link>05-notes/build-log-04-seed-tui.html</link><guid isPermaLink="false">05 notes/build-log-04-seed-tui.md</guid><pubDate>Wed, 29 Apr 2026 07:49:05 GMT</pubDate></item><item><title><![CDATA[build-log-03-seed-daemon]]></title><description><![CDATA[Tasks: TASK-006 · TASK-007 · Wave: 3The daemon: IPC server, state owner, event-log writer (TASK-006); scheduler tick + OS notifications (TASK-007). This is where <a data-href="event-sourcing" href=".html" class="internal-link" target="_self" rel="noopener nofollow">event-sourcing</a> meets the wire — commit() is the single writer, <a data-href="snapshot-and-replay" href=".html" class="internal-link" target="_self" rel="noopener nofollow">snapshot-and-replay</a> is the startup pattern, and the <a data-href="reminder-lifecycle" href=".html" class="internal-link" target="_self" rel="noopener nofollow">reminder-lifecycle</a> tick lives in schedule.rs. Initial pass: 155/155 tests green; after Wave 3.1 fixes, 158/158.
Notifier trait + MockNotifier — notify-rust::Notification::show() is never called in tests. DesktopNotifier wraps in spawn_blocking, swallows errors as warn! rather than propagating. The daemon never crashes on a failed OS notification.
<br>ReminderNotified event for debounce — rather than mutable daemon state outside the event log, we emit ReminderNotified per fire (keeping the <a data-href="event-sourcing" href=".html" class="internal-link" target="_self" rel="noopener nofollow">event-sourcing</a> invariant that all state changes flow through apply_event). The fold stores last_notified_ms on the runtime. Debounce check: last_notified_ms &gt; last_done_ms — silenced for the entire current due window until completion bumps last_done_ms. (Wave 3.1 wired the last_done_ms bump that this scheme depends on; see fixes below.)
#[serde(default)] on last_notified_ms — old snapshots / event logs deserialize as 0 (= never notified) rather than crashing. Forward-compat freebie.
XP drain max(1) — xp_drain() returns 35 (= 0.35 ×100). Integer division to 35 / 100 = 0 would make the mechanic invisible. Floor at 1 XP per overdue tick. Exact fractional accumulation deferred.
Periodic timer reuses the action channel — the 30s tick sends Action::TriggerReminderNow { reminder_id: None } through the same command channel, keeping the daemon's main loop to one match. request_id = 0 signals "no response needed."
interprocess GenericNamespaced — Windows pipe at \\.\pipe\seed-daemon-&lt;USERNAME&gt;, Unix abstract socket. socket_name() is cfg-guarded. Both are OS-cleaned on process exit, so stale-socket cleanup is a non-issue in this config.
<br>Atomic snapshot — write → .tmp → rename. Windows fs::rename over an existing file can fail if the target is locked; for v0 acceptable, and far safer than overwriting in place. (The non-atomic Windows case is tracked as TASK-027 in <a data-href="v0-1-0-punch-list" href="08-specs/v0-1-0-punch-list.html" class="internal-link" target="_self" rel="noopener nofollow">v0-1-0-punch-list</a>.)
Active-hours boundary is exclusive end — now_hour &gt;= start &amp;&amp; now_hour &lt; end. Hour 22 is excluded from (7, 22), matching the JSX reference. F4 — path.with_extension("tmp") produces snapshot.tmp, not snapshot.json.tmp. Behaviour correct (atomic), docstring lied; fixed in commentary later.
<br>F6 — Snooze silences the whole due window: snooze sets snoozed_until_ms but last_notified_ms &gt; last_done_ms stays true forever (until Complete bumps last_done_ms), so no second notification fires after snooze expires. Tracked under the snooze workstream — see <a data-href="overdue-rollover" href="08-specs/overdue-rollover.html" class="internal-link" target="_self" rel="noopener nofollow">overdue-rollover</a> and TASK-013 in <a data-href="v0-1-0-punch-list" href="08-specs/v0-1-0-punch-list.html" class="internal-link" target="_self" rel="noopener nofollow">v0-1-0-punch-list</a>.
F9 — commit() holds the state write-lock and event-log mutex across fsync. Slow disk stalls all readers. Acceptable at v0 scale.
F10 — Spawn-daemon integration test deferred to Wave 5 (TASK-011), where it landed as tests/smoke.rs. Per-connection response channels — fixed in 3.1 (was originally deferred to Wave 4).
Exact fractional XP drain — Wave 4+.
<br>Integration test — Wave 5 (<a data-href="build-log-05-docs-and-smoke" href="05-notes/build-log-05-docs-and-smoke.html" class="internal-link" target="_self" rel="noopener nofollow">build-log-05-docs-and-smoke</a>). <br><a data-href="build-log-04-seed-tui" href="05-notes/build-log-04-seed-tui.html" class="internal-link" target="_self" rel="noopener nofollow">build-log-04-seed-tui</a> — TUI client built against this IPC contract.
<br><a data-href="overdue-rollover" href="08-specs/overdue-rollover.html" class="internal-link" target="_self" rel="noopener nofollow">overdue-rollover</a> — later spec evolving the Overdue → drain branch into auto-skip.
<br><a data-href="event-sourcing" href=".html" class="internal-link" target="_self" rel="noopener nofollow">event-sourcing</a>, <a data-href="snapshot-and-replay" href=".html" class="internal-link" target="_self" rel="noopener nofollow">snapshot-and-replay</a>, <a data-href="reminder-lifecycle" href=".html" class="internal-link" target="_self" rel="noopener nofollow">reminder-lifecycle</a> — the invariants this wave wires up.
]]></description><link>05-notes/build-log-03-seed-daemon.html</link><guid isPermaLink="false">05 notes/build-log-03-seed-daemon.md</guid><pubDate>Wed, 29 Apr 2026 07:48:24 GMT</pubDate></item><item><title><![CDATA[build-log-02b-glyph-renderer]]></title><description><![CDATA[Tasks: TASK-004 · Wave: 2Pure renderer for the mandala glyph (the substrate of <a data-href="glyph-layer-composition" href=".html" class="internal-link" target="_self" rel="noopener nofollow">glyph-layer-composition</a>). ~1,200 lines across glyph.rs, tests/glyph.rs, glyph_golden.txt. Pulled ratatui into seed-core (the only crate that needs Buffer) — <a data-href="pure-core" href=".html" class="internal-link" target="_self" rel="noopener nofollow">pure-core</a> still holds: no I/O, no clock, just (traits, seed, target) → GlyphFrame. Initial pass: 127/127 tests green; after Wave 2B.1 fixes, 131/131. Release perf: 1.4ms/frame at 159×79 — 11× headroom against the 16ms target.
Hash function ports JSX exactly — u32::wrapping_mul mirrors JS &gt;&gt;&gt; 0 unsigned 32-bit wrap. Same constants, same distribution, same (h % 10000) / 10000 output. Wave 2B.1 widened the seed handling to a full u64 (splitmix-folded) after the inspector flagged a u32 truncation collision.
Weighted-char distribution ports JSX exactly — asymmetric Gaussian-like spread, u^1.9 center bias, 5%-chance low-dip at high v, 1%-chance high-poke at low v. This is the "even 99 has some non-max chars" texture the brief calls for.
Linear-RGB blend, not oklab — avoids a new dep. Inspector flagged this as a quality regression vs JSX's HSL-space blend (zenith mid-tones look muddier); accepted as a tradeoff. Revisit if the published mandala feels off.
Character palettes by layer — braille for inner core / clarity, box-drawing for spine, diagonals (╱╲╳╪╫╭) for reach, block + half-block for warmth/aura/halo. Wave 2B.1 expanded the half-block sets after the inspector found ▄▌▐▙▚▛▜▞▟ missing from emitted output.
Grid sizing is exponential, target-aware, always odd — progress^0.6 for visible growth, max_w * progress^0.5 to fill canvas, capped at 160×80. Odd dimensions guarantee an exact center cell.
Determinism — only wrapping_mul, no RandomState / thread_rng / SystemTime. Verified byte-equal across in-process and process-to-process runs.
Symmetry mirror — H then V, port faithful to JSX. Wave 2B (initial) wrote ghost-empty cells from weighted_char returning ' ', which left the grid asymmetric where both halves were already non-space; Wave 2B.1 made the mirror unconditional and the tests now assert char-level equality.
Branch cap — JSX's branchLen = min(t * 0.35, armLen * 0.2) capped at 12 to bound the inner loop. Real values stay ≤ ~7 at 160×80; the cap is a safety guard for larger future canvases. Linear-RGB vs oklab quality regression — accepted; revisit if the rendered colours feel off.
golden_snapshot line-ending fragility on Windows — no .gitattributes pin. Watch for CRLF flake on first-time Windows checkouts.
pick_size clamps below base_w on tiny targets — silent; intentional.
The clarity ring formula places last_ring_r2 ≈ 42.5 in non-square coordinates, but the maximum visual distance in the grid is only ~29.5 (0.58 x-axis compression). Space/Depth/Warmth/Halo primary rendering zones fall outside the visible grid at this scale. Wave 2B.1 added secondary grid-bounded scatter passes at max_r * 0.45–0.68 for each. Conservative fix; primary geometry works correctly at 160×80.
<br><a data-href="build-log-03-seed-daemon" href="05-notes/build-log-03-seed-daemon.html" class="internal-link" target="_self" rel="noopener nofollow">build-log-03-seed-daemon</a> — daemon (TASK-006/007).
<br><a data-href="glyph-expansion" href="08-specs/glyph-expansion.html" class="internal-link" target="_self" rel="noopener nofollow">glyph-expansion</a> — later spec evolving silhouette + orbit-card layout (designs A/B/C, all shipped).
]]></description><link>05-notes/build-log-02b-glyph-renderer.html</link><guid isPermaLink="false">05 notes/build-log-02b-glyph-renderer.md</guid><pubDate>Wed, 29 Apr 2026 07:48:00 GMT</pubDate></item><item><title><![CDATA[build-log-02a-seed-core-foundation]]></title><description><![CDATA[Tasks: TASK-002 · TASK-003 · TASK-005 · Wave: 2Domain types + pure logic, the events module, and the config module. Establishes <a data-href="pure-core" href=".html" class="internal-link" target="_self" rel="noopener nofollow">pure-core</a> (clock + paths injected, no I/O in the lib), <a data-href="event-sourcing" href=".html" class="internal-link" target="_self" rel="noopener nofollow">event-sourcing</a> (single fold + replay), and the first cut of the <a data-href="reminder-lifecycle" href=".html" class="internal-link" target="_self" rel="noopener nofollow">reminder-lifecycle</a> state machine and <a data-href="tier-progression" href=".html" class="internal-link" target="_self" rel="noopener nofollow">tier-progression</a> table.~2,145 lines across crates/seed-core/: levels.rs, domain.rs, state.rs, events.rs, config.rs, paths.rs, plus integration test files for each. Cargo deps: dirs, toml. Initial pass: 85/85 tests green; after the Wave 2A.1 fixes below, 114/114.
ID newtypes hold String, not &amp;'static str — serde cannot derive Deserialize for newtypes wrapping &amp;'static str without a custom impl. Static catalog structs (Category, Reminder) keep &amp;'static str for static items; runtime types use String-based newtypes (TraitId, ReminderId, CategoryId) with From&lt;&amp;str&gt; impls.
<br>OSRS XP table via OnceLock&lt;Vec&lt;u64&gt;&gt; — matches JSX buildXpTable bit-exact at L2/10/…/92/99. Lvl 99 = 13,034,431 (later rescaled to 1,303,443 in Wave 6, see <a data-href="build-log-06-xp-calibration" href="05-notes/build-log-06-xp-calibration.html" class="internal-link" target="_self" rel="noopener nofollow">build-log-06-xp-calibration</a>).
xp_drain() returns u32 scaled × 100 — JSX returns 0.35f; integer API avoids fp drift. Callers accumulate and divide. Awkward at the boundary — flagged as a contract risk against TraitXpChanged.delta units (no schema annotation forces the divide).
xp_reward is deterministic — JSX adds Math.random() jitter; removed for pure logic. Daemon can re-add at the application layer.
Snapshot tests rely on alphabetical key ordering — serde_json::Value::Object serialises keys alphabetically when routed through to_value → Map. Snapshot strings reflect this; renames still break, but anyone routing through to_string directly will see different output. Documented at the test site.
mid_journey_state is deterministic — JSX used Math.random() for streaks; replaced with i % 6 / i % 10 so the function is pure and testable. AC says match tier/XP, not random portions.
Tier thresholds are an architect invention — JSX doesn't specify them. Set to round numbers (avg level × 9 traits). total_level=149 (mid-journey) lands in Bloom, matching the backlog's "FROND/BLOOM" target. These thresholds are not in the locked spec; future re-tuning would silently rewrite TierChanged event semantics. events-schema.md deferred to Wave 5 (TASK-011).
TASK-004 (glyph renderer) split into Wave 2B.
last_done_ms is not stamped in apply_event(ReminderCompleted) — daemon stamps it when writing the event. Avoids clock injection in the fold.
The Wave 2A inspector pass blocked on contracts that downstream waves would lock in. Wave 2A.1 patched the load-bearing items before Wave 2B opened.These were called out but not patched in 2A.1; carried forward as risks the downstream waves were aware of:
<br>F8 — xp_drain × 100 scale is undocumented in the wire schema (TraitXpChanged.delta has no unit annotation). Cross-component drift risk; closed in Wave 5 when <a data-href="events-schema" href="02-references/events-schema.html" class="internal-link" target="_self" rel="noopener nofollow">events-schema</a> landed.
<br>F11 — Duplicate seed_home API (paths::seed_home and config::seed_home_path). Tracked as TASK-018 in <a data-href="v0-1-0-punch-list" href="08-specs/v0-1-0-punch-list.html" class="internal-link" target="_self" rel="noopener nofollow">v0-1-0-punch-list</a> (which is also the work that restores the <a data-href="pure-core" href=".html" class="internal-link" target="_self" rel="noopener nofollow">pure-core</a> invariant for seed_core::config).
F13 — level_for_xp is O(99); binary_search would close it. Hot path is fine in practice; left as a perf nit.
F15 — Tier thresholds remain a free-form invention rather than a locked spec.
<br>F17–F23 — Non-blocking shape/style/coverage nits (silent ConfigChanged ignores, State::log unbounded VecDeque, to_envelope swallowing serialise errors, missing apply_event test coverage). Most are absorbed by later waves; the residue is in the <a data-tooltip-position="top" aria-label="v0-1-0-punch-list > Minor / nit cleanup batch" data-href="v0-1-0-punch-list#Minor / nit cleanup batch" href="08-specs/v0-1-0-punch-list.html#Minor / nit cleanup batch" class="internal-link" target="_self" rel="noopener nofollow">nit batch</a>. <br><a data-href="build-log-02b-glyph-renderer" href="05-notes/build-log-02b-glyph-renderer.html" class="internal-link" target="_self" rel="noopener nofollow">build-log-02b-glyph-renderer</a> — Wave 2B (TASK-004) opened after these fixes landed.
]]></description><link>05-notes/build-log-02a-seed-core-foundation.html</link><guid isPermaLink="false">05 notes/build-log-02a-seed-core-foundation.md</guid><pubDate>Wed, 29 Apr 2026 07:47:50 GMT</pubDate></item><item><title><![CDATA[v0-mvp]]></title><description><![CDATA[The locked initial-release plan, executed across six waves between 2026-04-22 and 2026-04-27. This milestone captures the historical record; active follow-up work lives in <a data-href="v0-1-0-punch-list" href="08-specs/v0-1-0-punch-list.html" class="internal-link" target="_self" rel="noopener nofollow">v0-1-0-punch-list</a> and <a data-href="BACKLOG.kanban" href="00-index/backlog.kanban.html" class="internal-link" target="_self" rel="noopener nofollow">BACKLOG.kanban</a>.The mandala is the central reward surface. From the original chat:
"99 in all skills should be visually intense"
"think bigger.. exponential"
"weighting across each spectrum; even 99 should have some of the non max characters"
"fractal influences go both micro and macro"
Push ratatui hard:
Braille (⠀–⣿) for sub-cell density in the inner core
Block + half-block (░▒▓█▀▄▌▐▖▗▘▙▚▛▜▝▞▟) for outer rings/petals/aura
Box-drawing for structural spokes
Per-cell truecolor (24-bit, with 256-color fallback)
Multi-trait layered hues (warm core / cool flow / accent rings / violet depth)
Animation: per-frame char swap shimmer + hue rotation at zenith
Deterministic from (traits, seed) <br>Wave: 1 · Status: shipped · Notes: <a data-href="build-log-01-workspace-bootstrap" href="05-notes/build-log-01-workspace-bootstrap.html" class="internal-link" target="_self" rel="noopener nofollow">build-log-01-workspace-bootstrap</a>
AC: cargo build succeeds at workspace root; each crate has stub lib.rs / main.rs that compiles; workspace Cargo.toml declares 3 members + shared [workspace.dependencies]; seed-daemon and seed-tui depend on seed-core via path; .gitignore covers target/, sandbox dirs, IDE crap; Rust 2024 edition. <br>Wave: 2 · Status: shipped · Notes: <a data-href="build-log-02a-seed-core-foundation" href="05-notes/build-log-02a-seed-core-foundation.html" class="internal-link" target="_self" rel="noopener nofollow">build-log-02a-seed-core-foundation</a>
<br>AC: Trait, Category, Reminder types serde-derived; static CATEGORIES (9) and REMINDERS (20) tables matching wellness/data.jsx exactly; OSRS XP table built once via OnceLock matching JSX buildXpTable; xp_for_level, level_for_xp, level_progress, xp_to_next, level_norm, xp_reward, xp_drain; property tests (lvl 92 ≈ half of lvl 99; round-trip 1..=99); the <a data-href="reminder-lifecycle" href=".html" class="internal-link" target="_self" rel="noopener nofollow">reminder-lifecycle</a> enum (Off / Dormant / Due / Overdue) with overdue at &gt;1.5× interval; <a data-href="pure-core" href=".html" class="internal-link" target="_self" rel="noopener nofollow">pure-core</a> (no IO; clock injected); <a data-href="tier-progression" href=".html" class="internal-link" target="_self" rel="noopener nofollow">tier-progression</a> table SEED..ZENITH with tier_for(total_level) lookup. <br>Wave: 2 · Status: shipped · Notes: <a data-href="build-log-02a-seed-core-foundation" href="05-notes/build-log-02a-seed-core-foundation.html" class="internal-link" target="_self" rel="noopener nofollow">build-log-02a-seed-core-foundation</a>
<br>AC: Event enum covering reminder/trait/companion/config events, extensible via Event::Unknown { kind, data } fallback (the <a data-href="wire-versioning" href=".html" class="internal-link" target="_self" rel="noopener nofollow">wire-versioning</a> forward-compat hook); wire envelope { "v": 1, "ts": "...", "kind": "seed.&lt;ns&gt;.&lt;event&gt;", "data": {...} }; all kind strings namespaced under seed.*; apply_event(state, event) -&gt; state is the <a data-href="event-sourcing" href=".html" class="internal-link" target="_self" rel="noopener nofollow">event-sourcing</a> single fold; round-trip serde test for every variant; JSON shape locked by snapshot tests; documented in <a data-href="events-schema" href="02-references/events-schema.html" class="internal-link" target="_self" rel="noopener nofollow">events-schema</a>. <br>Wave: 2 · Status: shipped · Notes: <a data-href="build-log-02b-glyph-renderer" href="05-notes/build-log-02b-glyph-renderer.html" class="internal-link" target="_self" rel="noopener nofollow">build-log-02b-glyph-renderer</a> · See also: <a data-href="glyph-expansion" href="08-specs/glyph-expansion.html" class="internal-link" target="_self" rel="noopener nofollow">glyph-expansion</a>, <a data-href="glyph-layer-composition" href=".html" class="internal-link" target="_self" rel="noopener nofollow">glyph-layer-composition</a>
<br>AC: render_glyph(traits, seed, target) -&gt; GlyphFrame with the <a data-href="glyph-layer-composition" href=".html" class="internal-link" target="_self" rel="noopener nofollow">glyph-layer-composition</a> structural layers (core / clarity rings / reach arms+branches+macro tendrils / spine / flow / space / depth / resonance / warmth / aura / halo); multi-tier palettes; per-cell Color::Rgb; symmetry mirroring; golden-file test (byte-equal); 159×79 frame in &lt;16ms release; zenith only at all 9 traits ≥ 0.97; apply_to_buf writes directly into ratatui Buffer. <br>Wave: 2 · Status: shipped · Notes: <a data-href="build-log-02a-seed-core-foundation" href="05-notes/build-log-02a-seed-core-foundation.html" class="internal-link" target="_self" rel="noopener nofollow">build-log-02a-seed-core-foundation</a>
AC: Config { active_hours, snooze_min, palette, reminders, notif_style }; Default matches JSX initialState baseline; load(seed_home) reads ~/.seed/config.toml if present, merges over defaults; seed init scaffolds annotated default; SEED_HOME env override; round-trip toml + tempfile-backed integration test.
<br>Note: seed-core::config retained file I/O at v0, breaking <a data-href="pure-core" href=".html" class="internal-link" target="_self" rel="noopener nofollow">pure-core</a> for this one module; restoring purity is tracked as TASK-018 in <a data-href="v0-1-0-punch-list" href="08-specs/v0-1-0-punch-list.html" class="internal-link" target="_self" rel="noopener nofollow">v0-1-0-punch-list</a>. <br>Wave: 3 · Status: shipped · Notes: <a data-href="build-log-03-seed-daemon" href="05-notes/build-log-03-seed-daemon.html" class="internal-link" target="_self" rel="noopener nofollow">build-log-03-seed-daemon</a>
<br>AC: seedd owns canonical State; on startup performs <a data-href="snapshot-and-replay" href=".html" class="internal-link" target="_self" rel="noopener nofollow">snapshot-and-replay</a> (loads ~/.seed/snapshot.json then folds events.jsonl tail); periodic snapshot every N events or T seconds; accepts interprocess connections; length-prefixed JSON framing; Request { id, action } and StateDiff { events } message kinds; broadcasts every committed event; fsync after each event write; rejects malformed frames without panicking; single-instance lock; --foreground flag; structured logging via tracing; graceful Ctrl-C shutdown. <br>Wave: 3 · Status: shipped · Notes: <a data-href="build-log-03-seed-daemon" href="05-notes/build-log-03-seed-daemon.html" class="internal-link" target="_self" rel="noopener nofollow">build-log-03-seed-daemon</a>
<br>AC: tokio task wakes every 30s, fires notify-rust::Notification on Dormant→Due transition; debounced via last_notified_ms; respects active_hours; respects per-reminder snooze; at most one notification per reminder per due window; XP drain on overdue (now folded into the same tick — see <a data-href="overdue-rollover" href="08-specs/overdue-rollover.html" class="internal-link" target="_self" rel="noopener nofollow">overdue-rollover</a>); simulated-clock unit tests. <br>Wave: 4 · Status: shipped · Notes: <a data-href="build-log-04-seed-tui" href="05-notes/build-log-04-seed-tui.html" class="internal-link" target="_self" rel="noopener nofollow">build-log-04-seed-tui</a>
<br>AC: seed connects to daemon socket on startup, auto-spawns seedd if absent; subscribes to StateDiff, applies events through seed_core::apply_event to maintain local mirror — TUI is a <a data-href="event-sourcing" href=".html" class="internal-link" target="_self" rel="noopener nofollow">event-sourcing</a> reader, never a writer; sends user actions as Request; auto-reconnect with exponential backoff (cap 5s); IPC on a separate tokio task with channel handoff to UI thread; panic hook + raw-mode guard. <br>Wave: 4 · Status: shipped · Notes: <a data-href="build-log-04-seed-tui" href="05-notes/build-log-04-seed-tui.html" class="internal-link" target="_self" rel="noopener nofollow">build-log-04-seed-tui</a>
<br>AC: orbit pane + side panel (LIST / LEVELS / LOG / CONFIG tabs) + bottom command bar; orbit slot picker ports JSX faithfully; truecolor with 256/16 fallback per <a data-href="terminal-capability-ladder" href=".html" class="internal-link" target="_self" rel="noopener nofollow">terminal-capability-ladder</a>; braille fallback (also per <a data-href="terminal-capability-ladder" href=".html" class="internal-link" target="_self" rel="noopener nofollow">terminal-capability-ladder</a>); 5 palettes (sage/dusk/mist/ember/moss); --dev / SEED_DEV=1 appends a fifth TWEAKS tab; key bindings (/, Enter, q, Tab/Shift+Tab); command vocab covering all 20 verbs + /&lt;trait&gt; &lt;n&gt; debug + help; toast on completion; shimmer animation; clean resize. <br>Wave: 4 · Status: shipped · Notes: <a data-href="build-log-04-seed-tui" href="05-notes/build-log-04-seed-tui.html" class="internal-link" target="_self" rel="noopener nofollow">build-log-04-seed-tui</a>
AC: tracing + tracing-subscriber in both binaries with SEED_LOG= override; daemon logs to ~/.seed/seedd.log with daily rotation; TUI logs to ~/.seed/seed.log (suppressed below WARN unless SEED_LOG set); panic hook in TUI restores terminal then re-panics; truecolor + braille probes documented in README. <br>Wave: 5 · Status: shipped · Notes: <a data-href="build-log-05-docs-and-smoke" href="05-notes/build-log-05-docs-and-smoke.html" class="internal-link" target="_self" rel="noopener nofollow">build-log-05-docs-and-smoke</a>
<br>AC: README.md covers install, quickstart, config, event-schema pour-integration note, troubleshooting; <a data-href="events-schema" href="02-references/events-schema.html" class="internal-link" target="_self" rel="noopener nofollow">events-schema</a> documents every event variant with example JSON; build-log notes populated; tests/smoke.rs end-to-end test (spawn daemon to tempdir SEED_HOME, send Request, verify event written and broadcast). Wave: 4 · Status: deferred to v0.2.0
<br>The full editable CONFIG tab with toml_edit round-trip and ConfigChanged reload was deferred. v0.1.0 ships a read-only cut as TASK-016 in <a data-href="v0-1-0-punch-list" href="08-specs/v0-1-0-punch-list.html" class="internal-link" target="_self" rel="noopener nofollow">v0-1-0-punch-list</a>; the full editable surface returns in v0.2.0.
Wave 6 reshaped the XP economy to enforce a 1-year-to-99 contract uniformly across all 9 traits and pre-wired the two prestige systems' data models without UI surface. See:
<br><a data-href="xp-pacing" href="08-specs/xp-pacing.html" class="internal-link" target="_self" rel="noopener nofollow">xp-pacing</a> — rescaled OSRS curve, per-reminder XP rewards, pacing bands enforced by tests
<br><a data-href="prestige-integrate" href="08-specs/prestige-integrate.html" class="internal-link" target="_self" rel="noopener nofollow">prestige-integrate</a> — per-trait reset to 1 with persistent visual enhancement
<br><a data-href="prestige-focus" href="08-specs/prestige-focus.html" class="internal-link" target="_self" rel="noopener nofollow">prestige-focus</a> — tokens currency awarded every +99 cumulative levels gained
<br><a data-href="build-log-06-xp-calibration" href="05-notes/build-log-06-xp-calibration.html" class="internal-link" target="_self" rel="noopener nofollow">build-log-06-xp-calibration</a> — what shipped
<br><a data-href="build-log-06b-redline-fixes" href="05-notes/build-log-06b-redline-fixes.html" class="internal-link" target="_self" rel="noopener nofollow">build-log-06b-redline-fixes</a> — inspector findings + corrective patches
Each wave was paired with a governor (planning) → architect (build) → inspector (audit) loop. The build-log notes record the architect output and inspector findings; this milestone is the consolidated narrative.
<br><a data-href="v0-1-0-punch-list" href="08-specs/v0-1-0-punch-list.html" class="internal-link" target="_self" rel="noopener nofollow">v0-1-0-punch-list</a> — active follow-up work (the v0.1.0 punch list).
<br><a data-href="BACKLOG.kanban" href="00-index/backlog.kanban.html" class="internal-link" target="_self" rel="noopener nofollow">BACKLOG.kanban</a> — active board.
<br><a data-href="event-sourcing" href=".html" class="internal-link" target="_self" rel="noopener nofollow">event-sourcing</a>, <a data-href="pure-core" href=".html" class="internal-link" target="_self" rel="noopener nofollow">pure-core</a>, <a data-href="wire-versioning" href=".html" class="internal-link" target="_self" rel="noopener nofollow">wire-versioning</a>, <a data-href="reminder-lifecycle" href=".html" class="internal-link" target="_self" rel="noopener nofollow">reminder-lifecycle</a>, <a data-href="tier-progression" href=".html" class="internal-link" target="_self" rel="noopener nofollow">tier-progression</a>, <a data-href="snapshot-and-replay" href=".html" class="internal-link" target="_self" rel="noopener nofollow">snapshot-and-replay</a>, <a data-href="glyph-layer-composition" href=".html" class="internal-link" target="_self" rel="noopener nofollow">glyph-layer-composition</a>, <a data-href="terminal-capability-ladder" href=".html" class="internal-link" target="_self" rel="noopener nofollow">terminal-capability-ladder</a> — the durable concepts this milestone leans on.
]]></description><link>09-milestones/v0-mvp.html</link><guid isPermaLink="false">09 milestones/v0-mvp.md</guid><pubDate>Wed, 29 Apr 2026 07:47:26 GMT</pubDate></item><item><title><![CDATA[ADR]]></title><description><![CDATA[<a data-href="ADR.base" href="00-index/adr.html" class="internal-link" target="_self" rel="noopener nofollow">ADR.base</a>Chronological log of significant architectural decisions.]]></description><link>04-architecture/adr/adr.html</link><guid isPermaLink="false">04 architecture/adr/ADR.md</guid><pubDate>Wed, 29 Apr 2026 07:46:03 GMT</pubDate></item><item><title><![CDATA[CLAUDE]]></title><description><![CDATA[This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.This is an Obsidian documentation vault following the obsidian-docs-template canonical structure. It uses a Zettelkasten-inspired numbered directory layout and publishes to GitHub Pages via the Webpage HTML Export plugin.Directories use numbered prefixes (00-99). The canonical set is defined in the template's lib/common.sh. Do not create new numbered directories without updating the template definitions.Use the template repo's scripts to maintain vault health:# Audit vault against canonical structure
/path/to/obsidian-docs-template/validate.sh . # Preview structural fixes
/path/to/obsidian-docs-template/migrate.sh . --dry-run # Apply fixes (additive only — never deletes content)
/path/to/obsidian-docs-template/migrate.sh .
Always run validate.sh after migrate.sh to confirm the result.Notes use YAML frontmatter. Status-aware directories (architecture/adr, notes, specs) expect a status field:---
status: draft # draft | approved | implemented | superseded | active | archived
tags: []
---
Index files live in their respective content directories (e.g. 01 concepts/CONCEPTS.md) and use an index tag.
Write/edit in Obsidian
Export via Webpage HTML Export plugin (Ctrl/Cmd+P → "Export")
Commit the generated docs/ directory
Push — GitHub Pages serves from docs/
The export path is configured to point at the project root's docs/ directory.
Don't modify files under .obsidian/plugins/ — these are managed by Obsidian and the template migration scripts
Don't create numbered directories outside the canonical set without updating lib/common.sh in the template repo
Don't delete .gitkeep files — they preserve empty directory structure in git
Don't manually edit .base files — these are Obsidian-managed binary-ish JSON for database views
]]></description><link>claude.html</link><guid isPermaLink="false">CLAUDE.md</guid><pubDate>Wed, 29 Apr 2026 07:45:44 GMT</pubDate></item><item><title><![CDATA[v0-1-0-punch-list]]></title><description><![CDATA[Source-of-truth prose for the active follow-up work between v0 MVP and the v0.1.0 tag. Tracked on the <a data-tooltip-position="top" aria-label="BACKLOG.kanban" data-href="BACKLOG.kanban" href="00-index/backlog.kanban.html" class="internal-link" target="_self" rel="noopener nofollow">backlog board</a> as one card per task; details, AC, and reality notes live here.Flags per task: D = daemon usefulness, L = dopamine loop, F = friction, H = engineering hygiene.<br>A separate workstream owns overdue → reset (see <a data-href="overdue-rollover" href="08-specs/overdue-rollover.html" class="internal-link" target="_self" rel="noopener nofollow">overdue-rollover</a>); tasks below avoid overlap and mark coordination points.
Deps: TASK-009 (TUI layout shipped)
Reality: Action::Snooze { reminder_id, minutes } exists in seed-daemon/src/wire.rs + daemon.rs handler (~line 293) and is re-declared in seed-tui/src/client.rs:66. Zero TUI dispatch — grep finds no Snooze in app.rs, command.rs, input.rs, view/orbit.rs, view/skill_detail.rs.
AC: Keybinding (suggest s while a LIST or skill-detail row is selected, or verb form snooze &lt;word&gt; [minutes]) sends IpcAction::Snooze with default duration from Config::snooze_min (already loaded in app.rs:94).
Orbit pane card visually reflects snoozed_until_ms &gt; now_ms (dim + remaining minutes or "ZZZ").
One new test in seed-tui/tests/client.rs covers the parse / key-map path. Deps: TASK-006
Reality: Event::LevelUp is only emitted from daemon.rs::build_complete_events on user-initiated Action::Complete. schedule.rs emits ReminderNotified and drain-side TraitXpChanged only. Background-only users (TUI closed) get notifications but no XP, no level-ups — the loop is gated on the TUI.
AC (pick one before tagging): Option A (preferred): seed log &lt;verb&gt; CLI subcommand on the seed binary connects to the daemon, sends Action::Complete, prints the resulting XP/level diff, exits. No TUI required.
Option B (scope cut): README + OS notification body explicitly say "open seed to log completions"; remove "level up" framing from background-use sales copy. Whichever lands, README's Why + What you get must match. Deps: none (text + tiny CLI parse)
Reality: <br>README Commands lists 9 of 20 reminder verbs; --help lists all 20 but mis-orders them. ?&lt;skill&gt;, /random, /all &lt;n&gt; are implemented and undocumented in README and <a data-href="cli-flags" href="02-references/cli-flags.html" class="internal-link" target="_self" rel="noopener nofollow">cli-flags</a>.
README + TASK-009 promise seed --dev / SEED_DEV=1 gate; grep in crates/seed-tui returns zero matches for either. Ctrl+T tweaks panel is always live, including destructive RESET and unrealistic XP multipliers — exposed to all users. AC: README "Commands" enumerates all 20 verbs (grouped by category) plus ?&lt;skill&gt;, /random, /all &lt;n&gt;.
A test parameterised over REMINDERS.iter().map(|r| r.word) asserts every word appears in --help output (catches drift).
Either gate the tweaks panel behind SEED_DEV=1 / --dev (preferred — destructive surface) or remove dev-mode framing from README + TASK-009 wording. No middle ground. Deps: TASK-009; TASK-018 (config-purity move) enables this naturally
Scope cut: The full editable + toml_edit round-trip + ConfigChanged reload set is deferred to v0.2.0. v0.1.0 ships a read-only surface that turns hidden knowledge into discoverable knowledge.
AC: Add SideTab::Config variant + render fn. Cycle order becomes LIST → LEVELS → LOG → CONFIG → LIST.
Renders the resolved Config: active_hours, snooze_min, palette, notif_style, glyph_seed, per-reminder interval_min / enabled (scrollable).
Footer hint: "edit ~/.seed/config.toml and restart seedd to apply changes."
No edit affordance, no Ctrl+S, no ConfigChanged emission. Those land with the full editable spec in v0.2.0. <br>Deps: external workstream — see <a data-href="overdue-rollover" href="08-specs/overdue-rollover.html" class="internal-link" target="_self" rel="noopener nofollow">overdue-rollover</a>
<br>Status: data-model + scheduler shipped 2026-04-29: Event::ReminderSkipped carries was_snoozed: bool, scheduler emits skip with snooze leniency, state.traits_skipped aggregates per-trait stats. LEVELS-tab ▾N indicator and skill-detail "Skipped: …" line are described in <a data-href="overdue-rollover" href="08-specs/overdue-rollover.html" class="internal-link" target="_self" rel="noopener nofollow">overdue-rollover</a> and remain the open follow-up — verify they render before tagging.
AC: <br>Confirm the LEVELS / skill-detail render paths from <a data-href="overdue-rollover#Per-trait skipped surface" href="08-specs/overdue-rollover.html#Per-trait skipped surface" class="internal-link" target="_self" rel="noopener nofollow">overdue-rollover &gt; Per-trait skipped surface</a> are wired in seed-tui/src/view/.
The pre-existing overdue_reminder_drains_xp test was split per the spec; check the new tests landed in crates/seed-daemon/src/schedule.rs.
Coordinate the status-bar surface with TASK-026. Deps: none; unblocks TASK-016 cleanup
<br>Reality: seed-core/src/config.rs violates the <a data-href="pure-core" href=".html" class="internal-link" target="_self" rel="noopener nofollow">pure-core</a> invariant — load() does fs::read_to_string, scaffold_default() does create_dir_all + write, seed_home_path() reads env. Replay/golden tests depend on core purity.
AC: seed_core::config::parse(toml: &amp;str) -&gt; Config (or similar) does only TOML→struct.
File reads move to the daemon and to seed_tui::init (init scaffold).
Drop seed_core::config::seed_home_path (duplicates paths::seed_home, has zero callers).
Drop dev-dependencies.tempfile from seed-core/Cargo.toml once the I/O tests move out. Deps: none
<br>Reality: seed-tui/src/app.rs::push_log (~line 1003) writes to self.state.log directly. Eleven call sites across dispatch_tweak_action and submit_command. Violates the <a data-href="event-sourcing" href=".html" class="internal-link" target="_self" rel="noopener nofollow">event-sourcing</a> invariant — TUI is a reader; only the daemon's commit() writes through apply_event. These client-only log lines vanish on TUI restart.
AC: Introduce App.client_log: VecDeque&lt;LogEntry&gt; separate from State.log.
LOG view renders both (client + daemon) with a visual distinction.
state field on App becomes effectively read-only post-snapshot; flag any remaining direct mutation in review. Deps: none
Reality: seed-tui/src/client.rs:1-138 reimplements Message, Action, ResponseResult, read_frame, write_frame, MAX_FRAME, socket_name. The "daemon is binary-only" justification comment is false — seed-daemon already exposes a lib (Cargo.toml:8-10) and tests/smoke.rs imports from it. Three copies of socket_name exist.
AC: seed-tui depends on seed-daemon = { path = "..." } and use seed_daemon::wire::*.
Delete the duplicates in client.rs. Adjust the comment. Deps: TASK-020
<br>Reality: Daemon persistence routes unknown event kinds to Event::Unknown (preserving payload — the <a data-href="wire-versioning" href=".html" class="internal-link" target="_self" rel="noopener nofollow">wire-versioning</a> guarantee). TUI's app.rs::apply_envelope (~line 948) hand-rolls serde_json::from_value::&lt;CoreEvent&gt; and silently drops on Err. A TUI talking to a newer daemon desyncs without warning. Compounds with the missing prestige UI in TASK-024.
AC: Replace the hand-rolled deserialize with seed_core::events::from_envelope(env).
apply_event already handles Event::Unknown as a no-op — the reader path becomes correct by construction.
Add a test that feeds the TUI a synthetic future-kind envelope and asserts the local mirror still applies the rest of a batch. Deps: none, but pairs with TASK-021
Reality: Each event kind string is hard-coded in three places — #[serde(rename = ...)] per variant in seed-core/src/events.rs:31-135, is_known_kind (:198-218), event_kind (:220-245). Adding a new variant compiles clean but from_envelope routes the kind to Unknown. event_kind for Event::Unknown returns the literal "seed.unknown" and discards the original kind string — wrong.
AC: Define kinds once (e.g. const KINDS: &amp;[(EventDiscriminant, &amp;str)] or a derived accessor).
is_known_kind and event_kind are derived from that single source.
<br>Add a doc-test or integration test that asserts every variant in Event appears in <a data-href="events-schema" href="02-references/events-schema.html" class="internal-link" target="_self" rel="noopener nofollow">events-schema</a>. Deps: TASK-022 ideally
Status: doc-side fixes landed 2026-04-29 in the post-migration pass. Code-side enforcement (the test that fails when schema and Tier enum drift) remains.
Reality: <br><a data-href="events-schema" href="02-references/events-schema.html" class="internal-link" target="_self" rel="noopener nofollow">events-schema</a> lists 7 tiers SEED → … → ZENITH. seed-core/src/domain.rs:466-477 defines 10. Fixed: schema now lists all 10 with thresholds — see <a data-href="tier-progression" href=".html" class="internal-link" target="_self" rel="noopener nofollow">tier-progression</a>.
seed.trait.level_up schema does not mention old_level: u8. Fixed: documented with #[serde(default)] semantics.
Event::ReminderIntervalChanged (seed.reminder.interval_changed) missing from schema. Fixed: documented.
Prestige specs status: draft despite data models being implemented. Fixed: both flipped to partial with implementation banners.
<br>Backlog TASK-007 wording about XP drain being a "separate slower tick" was already corrected during the migration to <a data-href="v0-mvp" href="09-milestones/v0-mvp.html" class="internal-link" target="_self" rel="noopener nofollow">v0-mvp</a>. AC remaining: <br>One test asserts every Tier::name() appears in <a data-href="events-schema" href="02-references/events-schema.html" class="internal-link" target="_self" rel="noopener nofollow">events-schema</a> (or the doc is generated from the enum). Same for every Event variant kind string.
Pairs naturally with TASK-022 (single source of truth for event kinds): the test that closes both is the same shape — derive doc references from the enum, fail on drift. Deps: TASK-021
Reality: Event::TraitIntegrated, Event::FocusTokenEarned, Event::FocusPhaseActivated exist. State carries cumulative_levels_gained, tokens_spent, active_focus, trait_integrations, trait_enhancements. Daemon emits FocusTokenEarned correctly on level-up boundaries (daemon.rs:605-615). Grep prestige|FocusToken|TraitIntegrated|FocusPhase in seed-tui/src returns zero matches. Tokens accumulate silently; level-99 traits cannot integrate; the long-tail dopamine the specs lean on is dead.
<br>Status: prestige specs (<a data-href="prestige-integrate" href="08-specs/prestige-integrate.html" class="internal-link" target="_self" rel="noopener nofollow">prestige-integrate</a>, <a data-href="prestige-focus" href="08-specs/prestige-focus.html" class="internal-link" target="_self" rel="noopener nofollow">prestige-focus</a>) flipped to status: partial on 2026-04-29 with explicit "data model implemented, UI deferred to v0.2.0" banners. The cut is documented; the minimum-viable hint is still open work below.
AC: LEVELS tab footer (or status bar) shows tokens: N when tokens_available(state) &gt; 0.
At least one trait at lvl 99 surfaces an "INTEGRATE READY" hint.
These two are the v0.1.0 minimum. Full UI lives in v0.2.0. Deps: none
Reality: Event::TierChanged { from, to, total_level } exists with snapshot tests but no producer — apply_event is a no-op for it (events.rs:482), and no daemon path emits it. Grep confirms zero producers in seed-daemon/src. Tier crossings are the natural macro celebration moments (~9 across the whole companion lifetime); they're currently invisible.
AC: daemon.rs::build_complete_events (or a sibling) computes tier_for(old_total) vs tier_for(new_total) after a level-up; emits TierChanged on transition.
TUI shows a distinctive toast for tier changes (separate ToastKind from LevelUp).
Round-trip + emission test. Deps: coordinate with TASK-017
Reality: seed-tui/src/view/status_bar.rs:14 exposes a single any_overdue: bool; status bar reads "COMPANION WILTING" with no count, no list, no urgency gradient. Combined with the missing snooze (TASK-013) and overdue→reset reshape, the user has near-zero context.
AC: Status bar shows OVERDUE: N · SNOOZED: M · COMPLETED: K (or equivalent).
Coordinated with whatever shape TASK-017 lands. Deps: none
<br>Reality: the <a data-href="snapshot-and-replay" href=".html" class="internal-link" target="_self" rel="noopener nofollow">snapshot-and-replay</a> pattern relies on the event log being byte-honest; these issues poke holes in that contract. event_log.rs::count_lines (~:143-150) consumes reader.lines() regardless of Ok/Err. Bad-UTF-8 lines increment count by 1 but load_from (~:89-110) skips them with warn!. Snapshot encodes drifted event_count; on restart load_from(_, skip=N) skips past valid events. Subtle data loss.
snapshot_write (~:115-127) uses fs::rename(tmp, path) which is not atomic on Windows when the destination exists.
EventLog::append (~:71-74) has two identical #[cfg(target_os = "windows")] / #[cfg(not(...))] branches calling the same sync_data(). Dead branching. AC: count_lines returns Result&lt;usize, io::Error&gt; and counts only Ok lines (or is replaced by tracking the count alongside append).
Snapshot uses tempfile::persist (or a wrapper around MoveFileExW(MOVEFILE_REPLACE_EXISTING | MOVEFILE_WRITE_THROUGH)).
Delete the redundant cfg in append.
Test: corrupt-line-mid-log replay + restart preserves event_count. Deps: ideally lands after TASK-019/020/021 to avoid merge churn
Reality: seed-tui/src/app.rs is 1089 lines doing terminal setup + IPC subscribe + render orchestration + 8 sidebar nav handlers + command parser + tweaks dispatch + skill-detail dispatch + level-up toast policy + level computation + request-id alloc + a splitmix64 RNG. #[allow(dead_code)] on the struct (line 58) silences the linter wholesale.
seed-core/src/glyph.rs::render_glyph is 800 lines of inlined per-layer cell math (11 layers + symmetry mirror + frame materialization). A local fn rgb is even defined inside the function body — already an admission this should be modularized. AC: app.rs carved into at minimum app/input.rs, app/render.rs, app/dispatch.rs. Move random_trait_levels to a debug-only module. Replace the struct-level #[allow(dead_code)] with per-field attributes (or delete dead fields).
glyph.rs introduces a LayerRenderer trait or sibling module per layer; render_glyph becomes a short composition.
No behaviour change; golden snapshot must still byte-match. Run cargo test -p seed-core --test glyph as the sign-off. These are small enough to bundle as a single PR after the larger items land:
Status bar advertises [ Q ] quit while input.rs:77-78 says bare q is intentionally not quit in the main view. Fix the affordance text or harmonise the binding.
Six #[cfg(debug_assertions)] eprintln! calls inside seed_core::events::apply_event (:382-468) — corrupts TUI screen, breaks core purity. Return Result&lt;()&gt; from the validation arms and let the daemon log.
crossterm is double-pinned (workspace + seed-tui/Cargo.toml:26); futures slipped into seed-tui only (line 28). Lift to [workspace.dependencies].
view/toast.rs:69 width uses msg.len() (bytes); use unicode-width::UnicodeWidthStr::width (already a workspace dep).
IpcClient::send (client.rs:201-205) returns (), swallowing backpressure. At minimum return Result&lt;(), SendError&gt; so callers can choose to surface "command dropped during reconnect."
daemon::Action::Reset (daemon.rs:447-501) hand-rolls everything commit() does. Route through commit().
Dead pubs / dead-code: seed_core::glyph::weighted_char_pub, seed_core::config::seed_home_path (duplicates paths::seed_home), seed_core::paths::config_path (TUI's init.rs:11 reimplements it inline), view/title_bar.rs:29 let _ = tier_for(...), view/command_bar.rs:37 let _ = color, schedule.rs:91-92 discarded level.
crates/seed-core/tests/seed_home_env_override.rs:12-17 SAFETY note is wrong (not "tests are single-threaded by default"); rewrite to "this test binary contains exactly one test."
#[ignore] without note: crates/seed-core/tests/glyph.rs:339 (perf_glyph_159x79), :368 (dump_golden). Add a note (e.g. #[ignore = "perf, run with --ignored"]).
crates/seed-daemon/src/daemon.rs:71-72 order: create_dir_all after EventLog::open — if seed_home doesn't exist, snapshot_read fails first. Move create_dir_all above snapshot_read.
Trait-id strings hardcoded twice (seed-core/src/state.rs:130-167 vs seed-core/src/glyph.rs:155-163). Define once in domain.rs.
Pin-mark inconsistency: view/orbit.rs uses "*", view/side_panel.rs uses "★". Pick one (with the existing braille fallback pattern).
Triple KeyEventKind::Press | KeyEventKind::Repeat filter (app.rs:373-376, :408-411, input.rs:55-58). One helper. Event-kind discipline — TASK-022 + TASK-023 + TASK-025 are one thread; close them together so the schema, the Rust enum, and the producers stay in lockstep.
Forward-compat asymmetry — TASK-021 is a prereq for TASK-024; until the TUI stops dropping unknown events, adding the prestige UI surface paves over the bug.
Config purity ↔ CONFIG tab — TASK-018 is the natural moment to do TASK-016, since the TUI becomes the config consumer regardless. TASK-015 (docs / dev-flag) — text + tiny CLI parse, zero risk.
TASK-013 (snooze) — small, daemon side already there.
TASK-018 → TASK-016 (config purity, then read-only CONFIG tab).
TASK-020 → TASK-021 (single-source wire, then from_envelope in TUI).
TASK-022 + TASK-023 + TASK-025 (event-kind discipline thread).
TASK-024 minimum (token counter or explicit defer).
TASK-027 (event-log durability).
TASK-014 (loop closure decision — CLI or doc reframe).
TASK-019 (push_log separation).
TASK-026 + TASK-017 (coordinate overdue surface with overdue→reset workstream).
Minor / nit batch as one PR.
TASK-028 (carve modules) — last, to avoid merge churn. <br><a data-href="v0-mvp" href="09-milestones/v0-mvp.html" class="internal-link" target="_self" rel="noopener nofollow">v0-mvp</a> — historical plan for the v0 MVP (TASK-001..012, all shipped).
<br><a data-href="BACKLOG.kanban" href="00-index/backlog.kanban.html" class="internal-link" target="_self" rel="noopener nofollow">BACKLOG.kanban</a> — active board; one card per task here.
<br><a data-href="overdue-rollover" href="08-specs/overdue-rollover.html" class="internal-link" target="_self" rel="noopener nofollow">overdue-rollover</a> — the overdue → reset spec coordinated by TASK-017.
<br><a data-href="xp-pacing" href="08-specs/xp-pacing.html" class="internal-link" target="_self" rel="noopener nofollow">xp-pacing</a>, <a data-href="prestige-integrate" href="08-specs/prestige-integrate.html" class="internal-link" target="_self" rel="noopener nofollow">prestige-integrate</a>, <a data-href="prestige-focus" href="08-specs/prestige-focus.html" class="internal-link" target="_self" rel="noopener nofollow">prestige-focus</a> — referenced by TASK-023 / TASK-024.
<br><a data-href="events-schema" href="02-references/events-schema.html" class="internal-link" target="_self" rel="noopener nofollow">events-schema</a> — the wire-schema doc TASK-022 / TASK-023 reconcile.
<br><a data-href="event-sourcing" href=".html" class="internal-link" target="_self" rel="noopener nofollow">event-sourcing</a>, <a data-href="pure-core" href=".html" class="internal-link" target="_self" rel="noopener nofollow">pure-core</a>, <a data-href="wire-versioning" href=".html" class="internal-link" target="_self" rel="noopener nofollow">wire-versioning</a>, <a data-href="snapshot-and-replay" href=".html" class="internal-link" target="_self" rel="noopener nofollow">snapshot-and-replay</a> — the invariants several tasks here are restoring.
]]></description><link>08-specs/v0-1-0-punch-list.html</link><guid isPermaLink="false">08 specs/v0-1-0-punch-list.md</guid><pubDate>Wed, 29 Apr 2026 07:45:42 GMT</pubDate></item><item><title><![CDATA[glyph-expansion]]></title><description><![CDATA[Status: A, B, C all implemented. Vanilla zenith intentionally tuned at ~50%
visual headroom to leave room for future prestige / integration layers.Future "integration" unlocks (earned by resetting individual traits from
99 back to 1) will introduce solid block forms — the true core of the
companion — layered on top of the textured base mandala. The current
renderer therefore tunes vanilla level 99 across all 9 traits as a
wispy, complete-but-not-overwhelming field rather than a saturated
block fill. Concretely:
ZENITH_CAP = 0.5 — the smoothstep zenith blend caps at 0.5, so the
elliptical mask only lerps halfway to 1.0 at vanilla zenith. Corners
paint at half density; integration unlocks lifting the cap toward 1.0.
Outer scatter densities trimmed (Aura ×0.28, Halo-secondary ×0.11,
Warmth-secondary ×0.18, ...).
Char-selection capped at the wispy end of palettes (v * 0.55 for
Aura, v * 0.35 for Warmth-secondary) so block characters
('▄', '▌', '▐', '█') stay rare in vanilla. Integration relaxes the cap
to introduce solid forms.
Resonance density bumped (×0.26) and Depth-secondary density kept
generous (×0.13) — these layers use wispy chars (sparks, dots,
punctuation) and are the primary "wisp" providers.
Flow is reworked as rivers, not ocean (see Design A below).
The orbit pane renders a rectangular glyph silhouette regardless of trait
combination. At mid-to-high progress, the outer scatter passes (Aura, Halo
secondary, Warmth secondary, Depth secondary, Resonance) paint corner cells
densely because their gating uses d &gt; max_r * X where max_r is the corner
distance — every cell in the bounding box at sufficient d qualifies. The
backlog explicitly asks for "fractal influences both micro and macro" and "99
in all skills should be visually intense"; the current output reads as a
rectangle of texture, not a mandala.A second issue: orbit reminder cards float on a calculated ellipse and are
filtered by a clipping check. On smaller terminals or off-axis card positions
they get dropped silently, so the pane has cards appearing/disappearing as the
window resizes.
Glyph silhouette is round (or organically irregular), not rectangular.
Empty space at the corners is intentional and reserved for cards/menus.
Orbit cards live in fixed bounded boxes that are guaranteed to render.
No silent clipping. Card visibility never depends on glyph geometry.
Visual progression remains exponential: at zenith the glyph fills its
elliptical envelope densely; at low progress it stays sparse.
Render path remains pure / deterministic / cheap — see <a data-href="pure-core" href=".html" class="internal-link" target="_self" rel="noopener nofollow">pure-core</a> and the <a data-href="glyph-layer-composition" href=".html" class="internal-link" target="_self" rel="noopener nofollow">glyph-layer-composition</a> invariants. Animating the silhouette shape (the envelope is fixed per frame).
Replacing the ratatui Buffer rendering pipeline.
Per-trait silhouette morphing (a future possibility, not this spec).
The outer scatter layers are gated by a soft elliptical mask centred on the
glyph grid. Cells outside the ellipse get zero density; cells near the
ellipse boundary fade smoothly; cells deep inside paint at full density.Mask formula, evaluated per cell using the same aspect-corrected coords the
glyph already uses (dx = (x - cx) * 0.58, dy = y - cy):dx_max = cx * 0.58
dy_max = cy
r_norm² = (dx / dx_max)² + (dy / dy_max)²
base = max(0, 1 − r_norm²) // 1 at center, 0 at ellipse boundary
zenith_blend = smoothstep((progress − 0.85) / 0.15, 0..1)
mask = min(1, base + zenith_blend · (1 − base))
zenith_blend ramps from 0 below progress = 0.85 to 1 at progress = 1.0
(smoothstep). At low/mid progress mask = base (full elliptical falloff —
corners empty, reserved for cards). At zenith mask = 1 everywhere — outer
scatter passes paint the rectangular corners at full density and the glyph
fills the entire pane behind the cards.Aura's explicit upper-radius cap follows the same lerp:aura_re_max = 1.0 + zenith_blend · 0.55
With ZENITH_CAP = 0.5 this gives aura_re_max ≈ 1.275 at vanilla zenith
— enough to push past the ellipse but not all the way to the corners.Resonance and Warmth-secondary upper caps relax in the same way.Flow is the most-visible textural layer and was originally tuned too
aggressively. Reworked:
Gate: flow &gt; 0.35 (no flow until level ~35) — was flow &gt; 0.2.
Wave frequency bumped (* 0.65, * 0.85) — narrower channels per cycle.
Tight wave threshold: 1.0 - flow_eff * 0.34 where
flow_eff = flow - 0.35. At max flow only the top ~36% of wave heights
pass; at gate-clearing flow only the top ~9% pass. Result: thin defined
channels rather than parallel bands of fill.
Sparse density: 0.18 + flow_eff * 0.22.
Multiplied by ellipse_mask so flow stays inside the envelope —
prestige integration reserves the corner space for solid forms.
mask multiplies the density threshold for the following layers / passes:Structural inner layers (Core, Clarity rings, Reach arms+branches, Spine,
Flow diagonals) are not masked — they're already inherently radial /
angular and produce no rectangular fill on their own. The petal / band
primary passes (Warmth primary, Depth primary, Halo primary) use angular
sweeps that already fall on circles; they remain unmasked too.The all_layers_present_at_zenith regression test still passes because each
masked layer retains visible cells along the ellipse interior.
+ Removes rectangular silhouette in one stroke.
+ Pure additive: just multiplies an existing scalar, no new rendering
pass.
− At very low progress, outer layers were sparse anyway so the visual
delta is concentrated in mid-to-high progress states.
− Slightly fewer painted cells at zenith (corner cells removed).
Mitigated by the orbit-card layout reclaiming that real estate.
When reach &gt; 0.6, evenly-spaced anchor points along the inner ellipse
(at r_ellipse = 0.66, half-step rotated against the primary arms) host a
scaled-down mandala: a tiny braille core blob plus one ring drawn from
RING_CHARS. Each sub-mandala uses an offset seed
(seed.wrapping_add(900 + i * 53)) so its texture is distinct yet
deterministic.Tunables (in glyph.rs):sub_count = clamp(round(4 + (reach - 0.6) * 15), 4..10)
sub_re = 0.66 // anchor at 66% ellipse radius
sub_scale = scale * (0.30 + (reach - 0.6) * 0.50) // grows with reach
sub_core_r = max(sub_scale * 0.85, 1.0)
sub_ring_r = sub_scale * 1.9
sub_ring_w = max(sub_scale * 0.55, 0.6)
sub_max_r = sub_ring_r + sub_ring_w + 0.5
Anchors are placed in clockwise order at angles
a = i / sub_count * τ + π / sub_count so they sit between the primary
reach arms and don't visually compete with them.Constraints:
Each sub-mandala bounds-iterates over a (2·bb+1)² cell box where
bb = ceil(sub_max_r) — typically 3–5 — so total work is bounded by
sub_count × ~100 cells (≤ 1000 cell visits per frame at zenith).
Per-cell guard r_ellipse(parent) &lt; 0.98 clips sub-mandalas to the
parent ellipse so they never paint corners.
Sub-cells attribute to Layer::Reach (same layer as the parent
tendrils) — preserves the test invariant that all layers have ≥ 1 cell
at zenith without inventing a new layer.
Determinism preserved via the offset seed.
Skipped entirely when reach ≤ 0.6 so low-/mid-progress states are
unchanged. The mid_traits golden snapshot (reach=0.5) is therefore
unaffected. + True self-similarity matches the design intent literally.
+ Pushes visual ambition without enlarging the silhouette beyond what
the elliptical mask grants.
− Adds render cost proportional to arm_count × sub_radius² — small
but worth measuring on small terminals.
− More tuning surface (sub_scale, density, ring count). Should ship
with a regression test asserting "every macro tendril at high reach has
at least N painted sub-cells within R of its tip".
− Deferred until A is in and the silhouette is right; otherwise the
fractal detail gets buried in rectangle texture.
Replace the ellipse-floating layout with a fixed 3×3 grid, and let the
glyph paint the entire pane area with the cards floating on top in their
slots. As progress grows, the cards orbit inward and the mandala (driven by
SIZE_STEPS + the elliptical mask + zenith blend) expands to fill the freed
rim — so growth feels coupled. ┌──────────────────────────────────────┐ │ │ │ ┌────┐ TM ┌────┐ │ ← cards inset by `inset_v` │ │ TL │ . . . │ TR │ │ │ └────┘ └────┘ │ │ │ │ ┌────┐ glyph rim/aura/halo ┌────┐ │ │ │ ML │ also paints OUTSIDE │ MR │ │ │ └────┘ the orbit at zenith └────┘ │ │ │ │ ┌────┐ BM ┌────┐ │ │ │ BL │ │ BR │ │ │ └────┘ └────┘ │ │ │ └──────────────────────────────────────┘ ↑ ↑ inset_h inset_h card_w = 16, card_h = 3.
Glyph render rect = full area (no margin reservation). Cards stamp on
top in their slots; the cells beneath them are wasted glyph paint but the
cost is bounded by 8 × card_w × card_h ≈ 384 cells per frame.
Card inset is progress-driven (progress = total_level / 891): inset_h: 4 → 14 cells (linear)
inset_v: 1 → 3 cells (linear) Each of the 8 conceptual positions (TM/TR/MR/BR/BM/BL/ML/TL) becomes a
fixed top-left coordinate via the grid intersections (with the
progress-driven inset applied at the start/end edges).
Card readability against dense glyph texture: 1-cell horizontal padding (left + right only) around each card is
Clear-ed to empty cells — a soft "moat" separating the card from
glyph texture. Top/bottom butt directly against the mandala for a
tighter vertical silhouette.
Card body has a solid palette.bg2 backdrop (Block widget) so the
card text never reads through to the mandala underneath. Cell bg
persists when the subsequent set_line writes spans (spans only set
fg, leaving bg untouched). Slot count → position indices, walking clockwise from top-centre:responsive_slot_count(area.width) keeps its current thresholds (≥100 → 8,
≥70 → 6, else 4).Cards render after the glyph, so they always overwrite glyph cells in their
slot. The elliptical mask leaves those corners empty anyway, so visually
nothing competes.
+ Cards never disappear. No silent drops.
+ Glyph gains margin: with cards at corners not orbiting, glyph_margin
shrinks from (18, 5) to (card_w, card_h) = (16, 3), giving the glyph
more vertical real estate.
− Loses the "orbiting" metaphor — cards are static slots, not floating
satellites. The original ellipse spacing read as gentle motion when slot
counts changed. Mitigated by keeping the order-of-fill (catalog order)
consistent.
− On very narrow widths (&lt; ~50 cols), even fixed slots overlap. We
fall back to slot count 4 (compass) below width 70; below ~50 the centre
glyph area collapses but the cards still fit. Spec drafted.
Elliptical falloff mask in glyph.rs (design A) — shipped.
Fixed-grid card layout in orbit.rs (design C) — shipped, with a 6-cell
horizontal / 1-cell vertical inset so cards sit tight around the glyph
instead of flush against the pane frame.
Recursive macro fractal (design B) — shipped, gated on reach &gt; 0.6.
mid_traits (reach=0.5) golden snapshot remains valid; zenith
layer-presence test still passes. Per-trait silhouette warping (e.g. resonance pulls outer ring inward).
Animation of the elliptical envelope.
Card content layout (already fixed at icon + name + bar + verb).
Recursive nested sub-mandalas (this pass implements one level of
recursion; further levels would need their own seed offsets and an even
tighter sub_scale to remain readable at terminal cell density). <br><a data-href="glyph-layer-composition" href=".html" class="internal-link" target="_self" rel="noopener nofollow">glyph-layer-composition</a> — the underlying 11-layer structure this spec gates with the elliptical mask
<br><a data-href="pure-core" href=".html" class="internal-link" target="_self" rel="noopener nofollow">pure-core</a> — the invariant that lets golden-snapshot tests lock the renderer
<br><a data-href="prestige-integrate" href="08-specs/prestige-integrate.html" class="internal-link" target="_self" rel="noopener nofollow">prestige-integrate</a> — the future "solid block forms" the wispy zenith leaves headroom for
]]></description><link>08-specs/glyph-expansion.html</link><guid isPermaLink="false">08 specs/glyph-expansion.md</guid><pubDate>Wed, 29 Apr 2026 07:44:50 GMT</pubDate></item><item><title><![CDATA[PRS]]></title><description><![CDATA[PR history and merge records.<a data-href="PRS.base" href="00-index/prs.html" class="internal-link" target="_self" rel="noopener nofollow">PRS.base</a>]]></description><link>10-prs/prs.html</link><guid isPermaLink="false">10 PRs/PRS.md</guid><pubDate>Wed, 29 Apr 2026 07:44:37 GMT</pubDate></item><item><title><![CDATA[overdue-rollover]]></title><description><![CDATA[Bound the Overdue state in <a data-href="reminder-lifecycle" href=".html" class="internal-link" target="_self" rel="noopener nofollow">reminder-lifecycle</a> so missed cycles eventually expire instead of accumulating. Adds the seed.reminder.skipped event (with a was_snoozed flag for snooze-leniency), a tick-time threshold, and a per-trait traits_skipped aggregation surfaced in LEVELS + skill-detail. No ReminderState enum widening.<br>The <a data-href="reminder-lifecycle" href=".html" class="internal-link" target="_self" rel="noopener nofollow">reminder-lifecycle</a> machine is Off / Dormant / Due / Overdue and Overdue persists indefinitely until the user manually completes, snoozes, or disables the reminder. After a sleep window or a few unattended hours the TUI greets the user with a wall of muted-red OVRD cards across the orbit pane, side-panel LIST tab, and skill-detail overlay. XP also drains continuously while overdue with no upper bound.Recurring reminders are habits, not nags. A missed cycle should expire, the reminder should roll forward to its next opportunity, and the visible state should return to "upcoming" without user intervention.A reminder rolls forward when now_ms − last_done_ms &gt; 2 × interval_ms. Equivalently, when overdue_ms &gt; interval_ms using the value already computed in reminder_status_with_interval (crates/seed-core/src/domain.rs).This caps the OVRD-visibility window at exactly 0.5 × I per cycle:<br>The rule is uniform across reminders. Per-reminder grace overrides are deferred (see <a class="internal-link" data-href="#out-of-scope-deferred" href="#out-of-scope-deferred" target="_self" rel="noopener nofollow">Out of scope</a>).Off
Dormant [0, 1.0×I)
Due [1.0×I, 1.5×I)
Overdue [1.5×I, 2.0×I) ← max OVRD window ↓ Skip event ↓
Dormant [0, 1.0×I) (cycle restarts)
The enum stays four-valued. Skip is a daemon-side scheduler decision — it is not a derived state.The scheduler tick (crates/seed-daemon/src/schedule.rs) checks the threshold inside the existing ReminderState::Overdue arm. When the threshold is exceeded, the tick emits exactly one ReminderSkipped event for the reminder and short-circuits — no XP drain that tick.ReminderState::Overdue =&gt; { let interval_ms = rt.interval_min as i64 * 60 * 1000; let overdue_ms = -status.ms_left; if overdue_ms &gt; interval_ms { let missed = (overdue_ms / interval_ms) as u32; events.push(Event::ReminderSkipped { … }); continue; } if !in_active_hours { continue; } // active-hours drain gate // … existing XP drain …
}
A long-running daemon catches up tick-by-tick. A cold-boot daemon catches up on its first tick — even a 10×I gap is collapsed in a single Skip event with missed_cycles = 9.XP drain is now gated by in_active_hours so overnight idleness no longer bleeds XP. This was previously ungated (the original TASK-007 wording explicitly distinguished notification gating from drain gating); the change folds the two gates together. Notification gating is unchanged. Skip emission is not gated — state cleanup must run during sleep so the user wakes to a clean board.state.reminders[id].last_done_ms = at_ms // always
// only when was_snoozed = false:
state.reminders[id].streak = 0
state.reminders[id].total_missed += 1 // saturating
state.traits_skipped[trait].lifetime += 1 // saturating
state.traits_skipped[trait].recent.push(at_ms) // pruned to last 7d
<br>The reminder's next status compute returns Dormant with ms_left == interval_ms. Streak resets and per-trait skip aggregation populate only when the user did not snooze during the cycle — see <a class="internal-link" data-href="#snooze-leniency" href="#snooze-leniency" target="_self" rel="noopener nofollow">Snooze leniency</a>. total_missed (already on ReminderRuntime, previously never written) and the new traits_skipped map both start being populated by this change.The three OVRD render sites — crates/seed-tui/src/view/{orbit,side_panel,skill_detail}.rs — are unchanged. Once a Skip event lands and the state mirror catches up, the reminder reads as Dormant and the OVRD label disappears organically.Event::ReminderSkipped { at_ms: i64, // alphabetical order matches serde_json output missed_cycles: u32, // (overdue_ms / interval_ms) as u32 at emission reminder_id: ReminderId, #[serde(default)] was_snoozed: bool, // see Snooze leniency
}
<br>Wire kind: seed.reminder.skipped. Schema rule per <a data-href="wire-versioning" href=".html" class="internal-link" target="_self" rel="noopener nofollow">wire-versioning</a>: adding an event variant is non-breaking; older readers preserve it via Event::Unknown round-trip. Documented in <a data-href="events-schema" href="02-references/events-schema.html" class="internal-link" target="_self" rel="noopener nofollow">events-schema</a>. The fold mutation is one more arm in the <a data-href="event-sourcing" href=".html" class="internal-link" target="_self" rel="noopener nofollow">event-sourcing</a> apply_event function.ReminderRuntime.total_missed: u32 already exists in crates/seed-core/src/state.rs and is never written today. This change starts populating it. No struct migration.None. The threshold check is inline in the scheduler tick — it reuses status.ms_left from the existing reminder_status_with_interval call and a one-line interval_ms recompute.
This spec.
Add Event::ReminderSkipped variant in crates/seed-core/src/events.rs with serde rename = "seed.reminder.skipped" and was_snoozed: bool (with #[serde(default)]). Extend is_known_kind, event_kind, apply_event. Add TraitSkipStats and state.traits_skipped in state.rs.
Modify the ReminderState::Overdue arm in crates/seed-daemon/src/schedule.rs: emit Skip when over 2×I (with continue), passing was_snoozed = rt.snoozed_until_ms &gt; rt.last_done_ms; else gate drain by in_active_hours.
<br>Document the kind in <a data-href="events-schema" href="02-references/events-schema.html" class="internal-link" target="_self" rel="noopener nofollow">events-schema</a> (envelope shape, fields, threshold semantics, canonical effect, snooze-leniency split).
Tests: crates/seed-core/src/events.rs: round-trip (both was_snoozed true and false), byte-equal JSON snapshot, apply_reminder_skipped_* (resets last_done_ms, zeroes streak, increments total_missed and per-trait skip aggregation when was_snoozed = false; preserves streak/missed when was_snoozed = true; unknown id is no-op).
crates/seed-daemon/src/schedule.rs: split existing overdue_reminder_drains_xp into overdue_under_2x_drains_xp (last_done = now − 80 min) and overdue_over_2x_emits_skip_not_drain (last_done = now − 100 min); new auto_skip_reports_correct_missed_cycles, auto_skip_during_inactive_hours_still_fires, overdue_drain_gated_by_active_hours, auto_skip_then_status_returns_dormant, auto_skip_emits_was_snoozed_true_when_snooze_active_in_cycle, auto_skip_emits_was_snoozed_false_when_no_snooze_in_cycle. Event::ReminderSkipped carries a was_snoozed: bool field (#[serde(default)] so older log entries without it deserialize as false).The daemon computes it at emission: rt.snoozed_until_ms &gt; rt.last_done_ms — true if the user explicitly deferred at any point during this overdue cycle, regardless of whether the snooze has since expired.apply_event semantics:
rt.last_done_ms = at_ms — always (state cleanup is unconditional).
rt.streak = 0 — only if !was_snoozed.
rt.total_missed += 1 (saturating) — only if !was_snoozed.
Skip still fires at 2×I regardless — the snooze flag only affects the penalty, not whether rollover occurs. Design intent: coax users into consistency with a reasonable penalty, but keep friction low.State carries a traits_skipped: BTreeMap&lt;TraitId, TraitSkipStats&gt; field (#[serde(default)] for old-snapshot compat). TraitSkipStats holds:
lifetime: u32 — total non-snoozed skips since awakening.
recent: Vec&lt;i64&gt; — epoch-ms timestamps of recent skips, pruned at apply time to the last 7 days.
apply_event populates this when !was_snoozed: walks the static catalog (reminder → category → trait), increments lifetime, appends at_ms to recent, then prunes entries older than at_ms − 7d.TraitSkipStats::count_7d(now_ms) counts entries within the 7-day window relative to now.Two TUI render sites surface this:LEVELS tab — each per-trait row appends a ▾N indicator after the XP line when count_7d &gt; 0. Color: palette.fg (muted) for 1–2 skips; palette.due (amber) for 3+.Skill detail overlay — adds a Skipped: N (7d) · M lifetime line after the reminder list. Omitted entirely when lifetime == 0. When count_7d == 0 but lifetime &gt; 0, shows Skipped: M lifetime only. Same color tiers as LEVELS.A user-facing Action::Skip { reminder_id } for voluntary dismissal of a Due or Overdue reminder. Wiring is parallel to Action::Snooze in crates/seed-daemon/src/daemon.rs. The auto-skip path always increments total_missed when was_snoozed = false; manual skip should likely not — voluntary acknowledgement is not a missed cycle. That asymmetry argues for either a kind: SkipKind { Auto, Manual } field on the event, or two separate event variants (ReminderSkipped for auto, ReminderDismissed for manual). Decide when wiring the action.The uniform 2 × I rule gives long-cadence reminders generous OVRD windows: journal and reflect at 24-hour interval present 12 hours of OVRD before rollover. If lived experience shows that's too long for daily anchors — e.g. you should not see "OVRD" on yesterday's morning pages at noon today — add an optional grace_min: Option&lt;u32&gt; to the static Reminder catalog and use grace_min.unwrap_or(interval_min) as i64 * 60 * 1000 in the threshold check. Defer until lived signal.Today streak only ever advances forward, via ReminderCompleted. This change adds the first reset path (auto-skip with was_snoozed = false → streak = 0). A wider streak-policy review is its own design question:
Does completing late but before Skip preserve streak?
Does an explicit user snooze that crosses an active-hours boundary count as a missed day?
Are streaks per-reminder, per-trait, or per-day?
None of those are answered here.
<br>Pacing is unchanged but XP drain becomes finite. The existing pacing band test in crates/seed-core/tests/levels.rs is computed from on-time completion budgets and never models drain, so it does not reject this change. But effective XP-per-day under partial-adherence shifts upward (drain bounded to 0.5 × I per missed cycle, gated to active hours). The 1-year-to-99 contract from <a data-href="xp-pacing" href="08-specs/xp-pacing.html" class="internal-link" target="_self" rel="noopener nofollow">xp-pacing</a> is a perfect-adherence ceiling and stays correct; the median experience moves slightly closer to it.
missed_cycles for cold-boot replay. A cold daemon that finds a 10×I gap emits one Skip event with missed_cycles = 9. Replays produce identical state. If retroactive backfill of historical state is later added (e.g. importing legacy logs), care will be needed to ensure at_ms for the synthesized Skip events lines up correctly — but no such backfill is planned.
<br>Daily-anchored OVRD remains visible for half a day. See <a class="internal-link" data-href="#per-reminder-grace-override" href="#per-reminder-grace-override" target="_self" rel="noopener nofollow">Per-reminder grace override</a>. Acceptable for v1. <br><a data-href="xp-pacing" href="08-specs/xp-pacing.html" class="internal-link" target="_self" rel="noopener nofollow">xp-pacing</a> — the on-time XP curve this rollover implicitly preserves
<br><a data-href="prestige-focus" href="08-specs/prestige-focus.html" class="internal-link" target="_self" rel="noopener nofollow">prestige-focus</a> — token earnings derive from cumulative levels and are unaffected
<br><a data-href="prestige-integrate" href="08-specs/prestige-integrate.html" class="internal-link" target="_self" rel="noopener nofollow">prestige-integrate</a> — per-trait XP reset is unaffected
<br><a data-href="reminder-lifecycle" href=".html" class="internal-link" target="_self" rel="noopener nofollow">reminder-lifecycle</a> — the state machine this spec extends with the auto-skip transition
<br><a data-href="event-sourcing" href=".html" class="internal-link" target="_self" rel="noopener nofollow">event-sourcing</a> — the fold semantics that make the was_snoozed split tractable
<br><a data-href="cli-flags" href="02-references/cli-flags.html" class="internal-link" target="_self" rel="noopener nofollow">cli-flags</a> — SEED_DEV=1 TWEAKS tab is the manual lever for backdating last_done_ms to test rollover end-to-end
]]></description><link>08-specs/overdue-rollover.html</link><guid isPermaLink="false">08 specs/overdue-rollover.md</guid><pubDate>Wed, 29 Apr 2026 07:44:26 GMT</pubDate></item><item><title><![CDATA[MILESTONES]]></title><description><![CDATA[Release and milestone summaries. Each milestone consolidates the wave-level <a data-tooltip-position="top" aria-label="NOTES" data-href="NOTES" href="05-notes/notes.html" class="internal-link" target="_self" rel="noopener nofollow">build-log notes</a> into a single canonical record.
<br><a data-href="v0-mvp" href="09-milestones/v0-mvp.html" class="internal-link" target="_self" rel="noopener nofollow">v0-mvp</a> — initial release plan, executed across waves 1–6 (2026-04-22 → 2026-04-27). Locked decisions, task list, prestige pre-wiring.
<br>The v0.1.0 punch list lives in <a data-href="v0-1-0-punch-list" href="08-specs/v0-1-0-punch-list.html" class="internal-link" target="_self" rel="noopener nofollow">v0-1-0-punch-list</a> (spec) and <a data-href="BACKLOG.kanban" href="00-index/backlog.kanban.html" class="internal-link" target="_self" rel="noopener nofollow">BACKLOG.kanban</a> (board). It's not a milestone yet — promote it to one when the punch list closes and v0.1.0 is tagged.]]></description><link>09-milestones/milestones.html</link><guid isPermaLink="false">09 milestones/MILESTONES.md</guid><pubDate>Wed, 29 Apr 2026 07:44:00 GMT</pubDate></item><item><title><![CDATA[SPECS]]></title><description><![CDATA[Feature and component specifications.<a data-href="SPECS.base" href="00-index/specs.html" class="internal-link" target="_self" rel="noopener nofollow">SPECS.base</a>
<br><a data-href="xp-pacing" href="08-specs/xp-pacing.html" class="internal-link" target="_self" rel="noopener nofollow">xp-pacing</a> — the 1-year-to-99 contract: rescaled OSRS curve, per-reminder XP rewards, pacing bands enforced by tests
<br><a data-href="prestige-integrate" href="08-specs/prestige-integrate.html" class="internal-link" target="_self" rel="noopener nofollow">prestige-integrate</a> — per-trait reset to 1 with a persistent visual enhancement; cosmetic, no XP-rate change
<br><a data-href="prestige-focus" href="08-specs/prestige-focus.html" class="internal-link" target="_self" rel="noopener nofollow">prestige-focus</a> — tokens currency awarded every +99 cumulative levels gained; spending opens a phase that distributes a 4× XP bonus across 1–3 traits <br><a data-href="overdue-rollover" href="08-specs/overdue-rollover.html" class="internal-link" target="_self" rel="noopener nofollow">overdue-rollover</a> — bound Overdue at 2×I; auto-skip restores Dormant, gate XP drain by active hours <br><a data-href="glyph-expansion" href="08-specs/glyph-expansion.html" class="internal-link" target="_self" rel="noopener nofollow">glyph-expansion</a> — elliptical falloff mask, recursive macro fractal, fixed orbit-card grid (designs A/B/C, all shipped) <br><a data-href="v0-1-0-punch-list" href="08-specs/v0-1-0-punch-list.html" class="internal-link" target="_self" rel="noopener nofollow">v0-1-0-punch-list</a> — full prose for TASK-013..028 and the v0.1.0 cut order. The <a data-tooltip-position="top" aria-label="BACKLOG.kanban" data-href="BACKLOG.kanban" href="00-index/backlog.kanban.html" class="internal-link" target="_self" rel="noopener nofollow">kanban board</a> tracks active state; this doc is the source of truth for AC and reality notes.
]]></description><link>08-specs/specs.html</link><guid isPermaLink="false">08 specs/SPECS.md</guid><pubDate>Wed, 29 Apr 2026 07:43:48 GMT</pubDate></item><item><title><![CDATA[STORIES]]></title><description><![CDATA[Vision documents, project narratives, and manifestos.]]></description><link>07-stories/stories.html</link><guid isPermaLink="false">07 stories/STORIES.md</guid><pubDate>Wed, 29 Apr 2026 07:43:40 GMT</pubDate></item><item><title><![CDATA[NOTES]]></title><description><![CDATA[Fleeting development notes and working drafts. These may be promoted to concepts, guides, or specs as they mature, or folded into milestone records.<a data-href="NOTES.base" href="00-index/notes.html" class="internal-link" target="_self" rel="noopener nofollow">NOTES.base</a><br>Per-wave execution records from the v0 MVP build. They are transitive — pinned here while the wave history is fresh, then folded into <a data-href="v0-mvp" href="09-milestones/v0-mvp.html" class="internal-link" target="_self" rel="noopener nofollow">v0-mvp</a> over time.
<br><a data-href="build-log-01-workspace-bootstrap" href="05-notes/build-log-01-workspace-bootstrap.html" class="internal-link" target="_self" rel="noopener nofollow">build-log-01-workspace-bootstrap</a> — Wave 1 · TASK-001 · workspace + toolchain
<br><a data-href="build-log-02a-seed-core-foundation" href="05-notes/build-log-02a-seed-core-foundation.html" class="internal-link" target="_self" rel="noopener nofollow">build-log-02a-seed-core-foundation</a> — Wave 2 · TASK-002/003/005 · domain types, events, config (with inspector audit + 2A.1 fix pass)
<br><a data-href="build-log-02b-glyph-renderer" href="05-notes/build-log-02b-glyph-renderer.html" class="internal-link" target="_self" rel="noopener nofollow">build-log-02b-glyph-renderer</a> — Wave 2 · TASK-004 · glyph renderer
<br><a data-href="build-log-03-seed-daemon" href="05-notes/build-log-03-seed-daemon.html" class="internal-link" target="_self" rel="noopener nofollow">build-log-03-seed-daemon</a> — Wave 3 · TASK-006/007 · IPC server + scheduler
<br><a data-href="build-log-04-seed-tui" href="05-notes/build-log-04-seed-tui.html" class="internal-link" target="_self" rel="noopener nofollow">build-log-04-seed-tui</a> — Wave 4 · TASK-008/009/010 · TUI client + ratatui layout + cross-cutting shims
<br><a data-href="build-log-05-docs-and-smoke" href="05-notes/build-log-05-docs-and-smoke.html" class="internal-link" target="_self" rel="noopener nofollow">build-log-05-docs-and-smoke</a> — Wave 5 · TASK-011 · README, events-schema, smoke test
<br><a data-href="build-log-06-xp-calibration" href="05-notes/build-log-06-xp-calibration.html" class="internal-link" target="_self" rel="noopener nofollow">build-log-06-xp-calibration</a> — Wave 6 · XP recalibration + prestige pre-wiring
<br><a data-href="build-log-06b-redline-fixes" href="05-notes/build-log-06b-redline-fixes.html" class="internal-link" target="_self" rel="noopener nofollow">build-log-06b-redline-fixes</a> — Wave 6 · redline fixes from inspector review
]]></description><link>05-notes/notes.html</link><guid isPermaLink="false">05 notes/NOTES.md</guid><pubDate>Wed, 29 Apr 2026 07:43:34 GMT</pubDate></item><item><title><![CDATA[ARCHITECTURE]]></title><description><![CDATA[Architecture design documents and system overview.See <a data-href="ADR" href="04-architecture/adr/adr.html" class="internal-link" target="_self" rel="noopener nofollow">ADR</a> for the full ADR index.]]></description><link>04-architecture/architecture.html</link><guid isPermaLink="false">04 architecture/ARCHITECTURE.md</guid><pubDate>Wed, 29 Apr 2026 07:43:24 GMT</pubDate></item><item><title><![CDATA[GUIDES]]></title><description><![CDATA[Developer workflow and implementation guides.<a data-href="GUIDES.base" href="00-index/guides.html" class="internal-link" target="_self" rel="noopener nofollow">GUIDES.base</a>
<br><a data-href="cargo-cheatsheet" href="03-guides/cargo-cheatsheet.html" class="internal-link" target="_self" rel="noopener nofollow">cargo-cheatsheet</a> — cargo build / run / test / lint commands, useful env vars, glyph golden-snapshot refresh, troubleshooting
]]></description><link>03-guides/guides.html</link><guid isPermaLink="false">03 guides/GUIDES.md</guid><pubDate>Wed, 29 Apr 2026 07:43:10 GMT</pubDate></item><item><title><![CDATA[REFERENCES]]></title><description><![CDATA[Library API references and external documentation relevant to this project.<a data-href="REFERENCES.base" href="00-index/references.html" class="internal-link" target="_self" rel="noopener nofollow">REFERENCES.base</a>
<br><a data-href="cli-flags" href="02-references/cli-flags.html" class="internal-link" target="_self" rel="noopener nofollow">cli-flags</a> — seed and seedd CLI flags, subcommands, and environment variables (SEED_HOME, SEED_LOG, SEED_DEV, SEED_FORCE_ASCII, SEED_FORCE_256)
<br><a data-href="events-schema" href="02-references/events-schema.html" class="internal-link" target="_self" rel="noopener nofollow">events-schema</a> — wire-protocol contract: every event kind, JSON envelope, and the versioning policy that pour readers depend on
]]></description><link>02-references/references.html</link><guid isPermaLink="false">02 references/REFERENCES.md</guid><pubDate>Wed, 29 Apr 2026 07:43:04 GMT</pubDate></item><item><title><![CDATA[CONCEPTS]]></title><description><![CDATA[Atomic concept notes — durable, reusable knowledge about patterns and primitives that recur across seed. Each note is self-contained and linkable: when a spec or build-log mentions one of these patterns, it wikilinks here rather than re-explaining.<a data-href="CONCEPTS.base" href="00-index/concepts.html" class="internal-link" target="_self" rel="noopener nofollow">CONCEPTS.base</a>These wikilinks already appear across the vault as unresolved nodes in the graph. They mark patterns the existing docs lean on implicitly; the next concepts pass writes the canonical definitions.
<br><a data-href="event-sourcing" href=".html" class="internal-link" target="_self" rel="noopener nofollow">event-sourcing</a> — fold-based state, append-only event log, replay determinism. The single invariant that apply_event is the only state mutation, used by both daemon (writer) and TUI (reader).
<br><a data-href="pure-core" href=".html" class="internal-link" target="_self" rel="noopener nofollow">pure-core</a> — the no-I/O invariant in seed-core. Clock injected as now: DateTime&lt;Utc&gt;; paths injected as &amp;Path. Why golden tests + replay work.
<br><a data-href="wire-versioning" href=".html" class="internal-link" target="_self" rel="noopener nofollow">wire-versioning</a> — the { v, ts, kind, data } envelope; additive (new kind / new field with default) vs breaking (rename / retype) classification; Event::Unknown for forward-compat round-trip.
<br><a data-href="reminder-lifecycle" href=".html" class="internal-link" target="_self" rel="noopener nofollow">reminder-lifecycle</a> — the Off / Dormant / Due / Overdue state machine, the 1.0×I → 1.5×I → 2.0×I thresholds, and the auto-skip rollover that bounds the Overdue window.
<br><a data-href="tier-progression" href=".html" class="internal-link" target="_self" rel="noopener nofollow">tier-progression</a> — 10 tiers from total_level (sum of all 9 trait levels), tier_for(). Why integrate steps tier down (intentional).
<br><a data-href="glyph-layer-composition" href=".html" class="internal-link" target="_self" rel="noopener nofollow">glyph-layer-composition</a> — 11 layers, symmetry mirror, blend accumulators. How prestige-integrate enhancements stack additively on the per-trait layer contribution.
<br><a data-href="snapshot-and-replay" href=".html" class="internal-link" target="_self" rel="noopener nofollow">snapshot-and-replay</a> — daemon startup pattern (load snapshot → fold tail past skip_count), periodic snapshot every 100 events / 5 min, why apply_event purity makes this safe.
<br><a data-href="terminal-capability-ladder" href=".html" class="internal-link" target="_self" rel="noopener nofollow">terminal-capability-ladder</a> — truecolor → 256-cube → 16-color; braille → block-density via popcount. Env-driven probes (SEED_FORCE_ASCII, SEED_FORCE_256) as escape hatches.
When writing each note: lead with the invariant or rule the concept enforces, then a small example, then a "where it shows up" list of links back to the specs / build-logs that depend on it.]]></description><link>01-concepts/concepts.html</link><guid isPermaLink="false">01 concepts/CONCEPTS.md</guid><pubDate>Wed, 29 Apr 2026 07:42:55 GMT</pubDate></item><item><title><![CDATA[xp-pacing]]></title><description><![CDATA[The XP curve and per-reminder reward values that determine how long it takes to reach lvl 99 in each trait. This is the source of truth: changes to reminder cadences or XP rewards must keep the contract intact, enforced by the pacing band tests in crates/seed-core/tests/levels.rs.seed ports the OSRS XP curve verbatim (lvl 99 = 13,034,431 XP, lvl 92 ≈ half) but pairs it with a single global reward formula — base=55 × 0.55 / 1.35 / 2.0 for late / on-time / overdue — regardless of which reminder fired or which trait it feeds. The reminder cadences then implicitly determine each trait's XP/day, with extreme variance.Under perfect adherence (15 active hours/day, 74 XP per on-time completion):12× spread between fastest and slowest, even the fastest ~10 years. The OSRS curve is decorative — pacing is emergent accident.In OSRS, every training method has a deliberately balanced XP/hr (fishing 30–70k, slayer 30–100k, combat 80–500k) and lvl 99 is calibrated to ~200–400 hours of dedicated grinding. The curve only makes sense when paired with a per-action XP/hr contract. seed needs the same discipline.Lvl 99 = ~365 days at perfect adherence, uniform across all 9 traits. Depth and flow both pace to the same time-to-99; reflective traits earn larger rewards per (rarer) action to balance the budget.The OSRS table is rescaled by SCALE_DIVISOR = 10. Lvl 99 = 1,303,443 XP (was 13,034,431). The shape is preserved: lvl 92 ≈ half of lvl 99, exponential top-end. Absolute XP values become legible 2–4 digit numbers, not 4–7 digit ones.1,303,443 / 365 ≈ 3,571 XP/day
active_hours = 15 hr/day → 238 XP/hr/trait (central target)
central band = 200–280 XP/hr (±15% around 238)
A single ±15% band cannot hold all 9 traits because reminder cadences vary structurally. A single narrow band that passes flow (two intra-window reminders at moderate cadence) would reject clarity (one very-frequent 20-min reminder + one 4-hour anchor), even though each individual clarity reminder is within contract when measured per-reminder. The pacing test therefore uses per-trait bands derived from each trait's fire profile.clarity and space band rationale: their 4-hour anchor reminders (sun, rest) pay ~720 XP per fire to hit the XP/hr contract. At 3.75 fires/day each anchor reminder contributes ~2,700 XP/day on its own; the high-frequency companion (look at 45 fires/day × 60 XP = 2,700 XP/day) doubles the total. The wider ceiling is not "loose" — a 15% cadence drift in either reminder would fall outside even this wider band.The band floors (2700–3000) correspond to roughly 180–200 XP/hr — slightly below the ±15% floor to account for the lowest-cadence reminders occasionally missing active-hour windows without breaking the contract.The band is enforced by pacing_band_per_trait in crates/seed-core/tests/levels.rs. A reminder cadence change that pushes its trait outside its band fails CI.Tightened from 0.55 / 1.35 / 2.0 to 0.6 / 1.0 / 1.4. The previous 2× overdue bonus rewarded tardiness; the new schedule makes punctuality (1.0) the canonical reward and fans the multipliers symmetrically around it. overdue retains a small comeback bonus; late has a soft penalty.xp_per_completion is baked onto each Reminder in the static catalog (crates/seed-core/src/domain.rs). Derived once via:xp_per_completion = round(daily_budget / reminders_per_day_for_this_trait)
so each reminder contributes equal XP/hr to its trait, and per-trait totals sum to the daily budget.The reach verb is reused — it doubles as both the trait id (reach, the movement trait) and a reminder word under the warmth trait (reach out to a friend). Catalog disambiguates by (reminder_id, cat, trait_id); the parser routes the verb against REMINDERS.word.High-effort, low-cadence reminders (journaling, reading, reaching out) carry the chunky rewards. This feels right narratively — sitting with morning pages for 20 minutes is a heavier act than a sip of water — and the daily totals balance.
This spec.
Rescale OSRS table — add SCALE_DIVISOR constant in crates/seed-core/src/levels.rs. Update level_99_canonical test to assert 1,303,443. Other ratio-based tests pass unchanged.
Add xp_per_completion: u32 field on Reminder; populate all 20 entries from the table above.
Replace the global base in xp_reward with a per-reminder lookup: xp_reward(reminder: &amp;Reminder, opts) -&gt; u32. Tighten multipliers to 0.6 / 1.0 / 1.4.
Add pacing band test in crates/seed-core/tests/levels.rs: for each trait, sum (reminders_per_day × xp_per_completion) across its reminders, assert ∈ 200×15 .. 280×15 XP/day window.
Update all xp_reward callers (handful in daemon and TUI) to the new signature. The table assumes perfect adherence. Real users skip and snooze; effective time-to-99 will stretch. The contract is a ceiling, not the median experience.
Tightened multipliers (0.6 / 1.0 / 1.4) change vibe — overdue used to feel like a comeback bonus; now it's softer. If the comeback feel matters, swap to 0.6 / 1.0 / 1.2 and lean on streaks elsewhere. One constant.
sun and rest (4hr cadence) pay ~720 XP per fire. That's a big toast magnitude. Consider whether daily-anchored reminders deserve distinctive toast styling so the number reads as intentional. Streak bonuses, time-of-day modifiers, anchor-hour bonuses (focus prestige is the only multiplier wired — see <a data-href="prestige-focus" href="08-specs/prestige-focus.html" class="internal-link" target="_self" rel="noopener nofollow">prestige-focus</a>).
Asymmetric per-trait pacing. Uniform 1-year is the contract; revisit only after lived experience.
<br>Tier-table changes — <a data-href="tier-progression" href=".html" class="internal-link" target="_self" rel="noopener nofollow">tier-progression</a> is in level space and survives the rescale untouched.
<br>Reminder cadences themselves — they're the input to this contract, not the output. Cadence changes flow through <a data-href="reminder-lifecycle" href=".html" class="internal-link" target="_self" rel="noopener nofollow">reminder-lifecycle</a> and the per-trait pacing band test catches drift. <br><a data-href="prestige-integrate" href="08-specs/prestige-integrate.html" class="internal-link" target="_self" rel="noopener nofollow">prestige-integrate</a> — per-trait reset to 1 with persistent visual enhancement
<br><a data-href="prestige-focus" href="08-specs/prestige-focus.html" class="internal-link" target="_self" rel="noopener nofollow">prestige-focus</a> — tokens currency that activates a 4× bonus phase
<br><a data-href="reminder-lifecycle" href=".html" class="internal-link" target="_self" rel="noopener nofollow">reminder-lifecycle</a> — the state machine that determines on-time / late / overdue
<br><a data-href="tier-progression" href=".html" class="internal-link" target="_self" rel="noopener nofollow">tier-progression</a> — total-level → tier mapping, unaffected by the curve rescale
<br><a data-href="cli-flags" href="02-references/cli-flags.html" class="internal-link" target="_self" rel="noopener nofollow">cli-flags</a> — daemon and TUI flags relevant to dev/test of XP behavior
]]></description><link>08-specs/xp-pacing.html</link><guid isPermaLink="false">08 specs/xp-pacing.md</guid><pubDate>Wed, 29 Apr 2026 07:42:48 GMT</pubDate></item><item><title><![CDATA[cli-flags]]></title><description><![CDATA[Reference for seed (TUI), seedd (daemon), and the environment variables that influence both.seed # launch the TUI; auto-spawns seedd if not running
seed init # scaffold ~/.seed/config.toml from defaults
seed --version | -V # print version
seed --help | -h # print help
The 20 reminder verbs (one per reminder in the catalog), grouped by trait:The reach verb is reused — it is both the trait id and a reminder word under warmth (reach out to a friend). The parser routes against REMINDERS.word; trait lookups via /&lt;trait&gt; use the trait id.Plus help and debug commands:seedd # run detached; logs to &lt;seed_home&gt;/seedd.log with daily rotation
seedd --foreground # run attached; logs to stderr
seedd --version
seedd --help
The daemon refuses to start if another instance is already responding on its IPC socket (single-instance lock). It traps Ctrl+C for graceful shutdown — flushes the final snapshot and log before exiting.
SEED_LOG accepts the full EnvFilter syntax: SEED_LOG="seed_daemon=debug,seed_core=trace" is valid and only raises log level for those crates.
SEED_FORCE_ASCII and SEED_FORCE_256 are diagnostic overrides — the renderer auto-probes at startup and only swaps in fallbacks where the terminal lacks support. The env vars force the fallback even on capable terminals (useful for screenshots and tests).
SEED_HOME accepts an absolute or relative path. Relative paths are resolved against the process's working directory at startup.
# verbose daemon for development
SEED_LOG=debug cargo run -p seed-daemon -- --foreground # verbose TUI
SEED_LOG=debug cargo run -p seed-tui # run TUI against an isolated seed home (for testing)
SEED_HOME=/tmp/seed-test cargo run -p seed-tui # enable dev mode for the TUI (TWEAKS tab)
SEED_DEV=1 cargo run -p seed-tui # force ASCII fallback (e.g. for terminal screenshots)
SEED_FORCE_ASCII=1 cargo run -p seed-tui # stop a detached daemon on Windows
taskkill /IM seedd.exe /F <a data-href="xp-pacing" href="08-specs/xp-pacing.html" class="internal-link" target="_self" rel="noopener nofollow">xp-pacing</a> — XP curve and per-reminder rewards
<br><a data-href="prestige-integrate" href="08-specs/prestige-integrate.html" class="internal-link" target="_self" rel="noopener nofollow">prestige-integrate</a> — per-trait reset prestige (visual)
<br><a data-href="prestige-focus" href="08-specs/prestige-focus.html" class="internal-link" target="_self" rel="noopener nofollow">prestige-focus</a> — token-driven XP multiplier prestige
<br><a data-href="reminder-lifecycle" href=".html" class="internal-link" target="_self" rel="noopener nofollow">reminder-lifecycle</a> — the state machine the verbs drive transitions through
<br><a data-href="terminal-capability-ladder" href=".html" class="internal-link" target="_self" rel="noopener nofollow">terminal-capability-ladder</a> — what SEED_FORCE_* variables override
<br><a data-href="ARCHITECTURE" href="04-architecture/architecture.html" class="internal-link" target="_self" rel="noopener nofollow">ARCHITECTURE</a> — workspace layout and crate boundaries
]]></description><link>02-references/cli-flags.html</link><guid isPermaLink="false">02 references/cli-flags.md</guid><pubDate>Wed, 29 Apr 2026 07:42:32 GMT</pubDate></item><item><title><![CDATA[events-schema]]></title><description><![CDATA[The event log at ~/.seed/events.jsonl is the durable source of truth for reminder completions, level-ups, and state changes — seed's <a data-href="event-sourcing" href=".html" class="internal-link" target="_self" rel="noopener nofollow">event-sourcing</a> substrate. Each line is a JSON envelope:{ "v": 1, "ts": "&lt;rfc3339 timestamp&gt;", "kind": "seed.&lt;namespace&gt;.&lt;event&gt;", "data": { ... } }
Fields:
v: envelope version (currently 1). Breaking schema changes bump this.
ts: when the event happened, RFC 3339 UTC.
kind: dot-namespaced string. All current events are under seed.*.
<br>data: payload specific to the event kind. Unknown kinds are preserved verbatim on deserialize (via Event::Unknown) so downstream tools never lose data — the <a data-href="wire-versioning" href=".html" class="internal-link" target="_self" rel="noopener nofollow">wire-versioning</a> forward-compat invariant.
User (or auto-trigger) completed a reminder.{ "v": 1, "ts": "2026-04-22T12:00:00Z", "kind": "seed.reminder.completed", "data": { "at_ms": 1745000000000, "new_xp": 1234, "reminder_id": "water", "streak": 5, "trait_id": "flow", "xp_gained": 74 }
}
Fields:
reminder_id: the reminder completed (e.g. "water", "walk", "breathe").
trait_id: the wellness trait that received XP (e.g. "flow" for hydration).
xp_gained: XP awarded for this completion.
new_xp: total XP for trait_id after the award.
streak: consecutive completion count for this reminder.
at_ms: epoch milliseconds when the completion was recorded. Redundant with envelope ts but available for fast reads without timestamp parsing.
Daemon fired an OS notification for a due reminder.{ "v": 1, "ts": "2026-04-22T12:00:00Z", "kind": "seed.reminder.notified", "data": { "at_ms": 1745000000000, "reminder_id": "water" }
}
Fields:
reminder_id: the reminder that was notified.
at_ms: epoch milliseconds when the notification fired. Used internally by the scheduler as a debounce marker — no second notification fires for the same due window.
Daemon auto-skipped an overdue reminder that exceeded 2× its interval since last_done_ms. Rolls the reminder forward to a fresh Dormant cycle. Emitted regardless of active hours — state cleanup must run during sleep so the user wakes to a clean board.{ "v": 1, "ts": "2026-04-22T12:00:00Z", "kind": "seed.reminder.skipped", "data": { "at_ms": 1745000000000, "missed_cycles": 3, "reminder_id": "water", "was_snoozed": false }
}
Fields:
at_ms: epoch milliseconds when the skip was recorded. Written to last_done_ms so the next status compute returns Dormant with ms_left == interval_ms.
missed_cycles: number of full interval cycles that elapsed beyond the first ((overdue_ms / interval_ms) as u32). At exactly 2×I this is 1; at 10×I this is 9. Informational — not used in the fold.
reminder_id: the reminder that was auto-skipped.
was_snoozed: true when the user snoozed at some point during this overdue cycle (snoozed_until_ms &gt; last_done_ms at emission time). Suppresses the streak reset and total_missed increment — the user signalled intent, so the penalty is waived. #[serde(default)] so older log entries without this field deserialize as false.
Threshold: the scheduler fires this event when now_ms − last_done_ms &gt; 2 × interval_ms (equivalently, overdue_ms &gt; interval_ms where overdue_ms = −ms_left). Each tick emits at most one ReminderSkipped per reminder; skip and XP drain are mutually exclusive within a tick.Canonical effect:
state.reminders[reminder_id].last_done_ms ← at_ms (always)
state.reminders[reminder_id].streak ← 0 (when was_snoozed = false)
state.reminders[reminder_id].total_missed += 1 (saturating; when was_snoozed = false)
state.traits_skipped[trait_id].lifetime += 1 (when was_snoozed = false)
state.traits_skipped[trait_id].recent appended and pruned to last 7 days (when was_snoozed = false)
User deferred a reminder for a configurable period.{ "v": 1, "ts": "2026-04-22T12:00:00Z", "kind": "seed.reminder.snoozed", "data": { "reminder_id": "look", "snooze_min": 10, "until_ms": 9999999 }
}
Fields:
reminder_id: the reminder snoozed.
snooze_min: duration of the snooze in minutes.
until_ms: epoch milliseconds when the snooze expires.
User changed the cadence for a single reminder. Persisted via the event log and replayed by apply_event into ReminderRuntime::interval_min, so cadence overrides survive restarts without depending on ~/.seed/config.toml.{ "v": 1, "ts": "2026-04-22T12:00:00Z", "kind": "seed.reminder.interval_changed", "data": { "minutes": 60, "reminder_id": "water" }
}
Fields:
reminder_id: the reminder whose interval was changed.
minutes: the new interval in minutes. Replaces the previous interval_min for this reminder; the static catalog default is unchanged.
A reminder was toggled on or off.{ "v": 1, "ts": "2026-04-22T12:00:00Z", "kind": "seed.reminder.enabled", "data": { "reminder_id": "sit" }
}
{ "v": 1, "ts": "2026-04-22T12:00:00Z", "kind": "seed.reminder.disabled", "data": { "reminder_id": "sit" }
}
Fields:
reminder_id: the reminder affected.
A reminder was pinned to (or unpinned from) the orbit ring.{ "v": 1, "ts": "2026-04-22T12:00:00Z", "kind": "seed.reminder.pinned", "data": { "reminder_id": "walk" }
}
{ "v": 1, "ts": "2026-04-22T12:00:00Z", "kind": "seed.reminder.unpinned", "data": { "reminder_id": "walk" }
}
Fields:
reminder_id: the reminder affected. Pinned reminders appear in the orbit ring regardless of urgency.
A trait's XP was adjusted (gain or drain). Emitted for XP drain during overdue windows as well as gains from completions.{ "v": 1, "ts": "2026-04-22T12:00:00Z", "kind": "seed.trait.xp_changed", "data": { "delta": 55, "new_xp": 500, "trait_id": "core" }
}
Fields:
trait_id: the affected trait.
delta: XP change (positive = gain, negative = drain).
new_xp: total XP for the trait after the change.
A trait crossed a level boundary.{ "v": 1, "ts": "2026-04-22T12:00:00Z", "kind": "seed.trait.level_up", "data": { "new_level": 5, "new_xp": 1154, "old_level": 4, "trait_id": "reach" }
}
Fields:
trait_id: the trait that leveled up.
old_level (#[serde(default)]): the level immediately before this event. Carried so the fold can compute levels_gained = new_level - old_level without reading pre-event state — ReminderCompleted may already have written new_xp into state.traits in the same commit batch. Default 0 lets old log entries deserialize cleanly; the fold then treats old_level = 0 as a 1-level gain (safe undercount, not a loss).
new_level: the level reached (1–99).
new_xp: total XP at the moment of level-up.
<br>Note: seed.trait.level_up is emitted alongside seed.reminder.completed when the same completion causes a level boundary to be crossed. Both appear in the same StateDiff batch. The fold uses levels_gained to advance state.cumulative_levels_gained, which drives focus token earning — see <a class="internal-link" data-href="#seedfocustoken_earned" href="#seedfocustoken_earned" target="_self" rel="noopener nofollow"><code></code></a>seed.focus.token_earned.<br>The companion advanced to a new tier. See <a data-href="tier-progression" href=".html" class="internal-link" target="_self" rel="noopener nofollow">tier-progression</a> for the full ladder. Tiers are computed from total level (sum of all 9 trait levels). The progression is SEED → SPROUT → FROND → BLOOM → ORBIT → LATTICE → MANDALA → LUMEN → NEBULA → ZENITH (10 tiers; thresholds in seed-core/src/domain.rs::Tier::min_total_level).{ "v": 1, "ts": "2026-04-22T12:00:00Z", "kind": "seed.companion.tier_changed", "data": { "from": "Seed", "to": "Sprout", "total_level": 20 }
}
Fields:
from: previous tier name. PascalCase enum variant (e.g. "Seed", "Lumen", "Zenith").
to: new tier name. PascalCase enum variant.
total_level: sum of all 9 trait levels at the time of the change.
Tier thresholds (min_total_level):The companion was first created, or was reset to initial state via the tweaks panel.{ "v": 1, "ts": "2026-04-22T12:00:00Z", "kind": "seed.companion.awakened", "data": { "glyph_seed": 42 }
}
Fields:
glyph_seed: the deterministic seed used to generate the mandala glyph. Constant per companion lifetime; changes on reset.
A configuration value was changed at runtime (e.g. palette swap from the tweaks panel).{ "v": 1, "ts": "2026-04-22T12:00:00Z", "kind": "seed.config.changed", "data": { "key": "palette", "value": "dusk" }
}
Fields:
key: the configuration key changed (e.g. "palette", "snooze_min", "notif_style").
value: the new value (any JSON type — string for palette/notif_style, number for snooze_min).
A trait was integrated: reset from level 99 to 1, with a visual enhancement appended to its glyph. Emitted at explicit user request. apply_event validates the trait is at level 99 and ignores the event defensively otherwise.{ "v": 1, "ts": "2026-05-01T12:00:00Z", "kind": "seed.trait.integrated", "data": { "trait_id": "flow", "new_integrations": 1, "enhancement_id": "FlowSpiral" }
}
Fields:
trait_id: the trait being integrated (must be at level 99).
new_integrations: cumulative integration count for this trait after this event (1-based).
enhancement_id: the visual enhancement chosen at this integration. Current values: FlowSpiral, CoreEmber, SpineLattice, ReachBranch, ClarityRing, SpaceVeil, DepthAbyss, ResonanceChord, WarmthGlow. Serialized as PascalCase string.
Effect: sets trait XP to 0 (level → 1), increments trait_integrations[trait_id], appends enhancement_id to trait_enhancements[trait_id].A focus token was earned because cumulative trait levels gained crossed a 99-multiple boundary. Emitted from the daemon when it detects a level-up crosses the boundary; cumulative_levels_gained is updated deterministically in the apply_event fold for LevelUp so replays produce identical token totals.{ "v": 1, "ts": "2026-05-01T12:00:00Z", "kind": "seed.focus.token_earned", "data": { "new_balance": 1 }
}
Fields:
new_balance: available token balance after this award (cumulative_levels_gained / 99 - tokens_spent).
Note: apply_event for this event is a no-op on state — cumulative_levels_gained is updated by the LevelUp fold. This event exists as a wire signal for UI notification.User spent a focus token to activate a phase. apply_event validates that traits.len() matches the pattern's expected count and that at least one token is available; ignores the event otherwise.{ "v": 1, "ts": "2026-05-01T12:00:00Z", "kind": "seed.focus.phase_activated", "data": { "pattern": "Spread3x2", "traits": ["flow", "core", "spine"] }
}
Fields:
pattern: one of Concentrate1x4, Spread2x3, Spread3x2. Determines the arrow count per trait (3/2/1) and the XP multiplier (4×/3×/2×).
traits: trait IDs to allocate. Must contain exactly 1, 2, or 3 entries matching the pattern.
Effect: increments tokens_spent, replaces active_focus with a new FocusPhase using the specified pattern and arrow allocations. Prior active phase is discarded (no stacking). There is no seed.focus.phase_ended event — phases end only by being replaced.Any event with an unrecognised kind round-trips through Event::Unknown { kind, data }. The kind string and full data payload are preserved verbatim. Consumers must tolerate unknown kinds for forward-compat.Example (a hypothetical future event a v0 client would see as Unknown):{ "v": 1, "ts": "2026-04-22T12:00:00Z", "kind": "seed.future.thing", "data": { "x": 42 }
}
Simple tail (shell):tail -f ~/.seed/events.jsonl | jq -r 'select(.kind == "seed.reminder.completed") | "\(.ts) \(.data.reminder_id)"'
Count completions by reminder:jq -r 'select(.kind == "seed.reminder.completed") | .data.reminder_id' ~/.seed/events.jsonl | sort | uniq -c | sort -rn
For a Rust consumer, depend on seed-core and use seed_core::events::{from_envelope, EventEnvelope}:use seed_core::events::{from_envelope, EventEnvelope}; for line in std::fs::read_to_string("~/.seed/events.jsonl")?.lines() { let env: EventEnvelope = serde_json::from_str(line)?; let event = from_envelope(env)?; // match on event variants...
}
<br>The full rules and rationale live at <a data-href="wire-versioning" href=".html" class="internal-link" target="_self" rel="noopener nofollow">wire-versioning</a>. Summary:
Adding a new kind does NOT bump v. Consumers must tolerate unknown kinds (they round-trip as Unknown).
Adding a new field to an existing kind's data does NOT bump v. Consumers must tolerate extra fields (serde ignores unknown fields by default).
Removing or renaming a field, changing a field's type, or renaming a kind DOES bump v. A daemon reading events with an unknown major v will log an error and skip the line rather than corrupt state.
The current version is 1.]]></description><link>02-references/events-schema.html</link><guid isPermaLink="false">02 references/events-schema.md</guid><pubDate>Wed, 29 Apr 2026 07:42:18 GMT</pubDate></item><item><title><![CDATA[build-log-01-workspace-bootstrap]]></title><description><![CDATA[Tasks: TASK-001 · Wave: 1Bootstrapped the Cargo workspace: three-crate layout (seed-core lib, seed-daemon → seedd bin, seed-tui → seed bin), workspace-level dependency pinning, stable toolchain, rustfmt defaults.Workspace root: Cargo.toml, rust-toolchain.toml, rustfmt.toml, .gitignore. Per-crate: Cargo.toml + stub lib.rs / main.rs for all three crates.
Workspace-level dependency pinning — all shared crate versions declared in [workspace.dependencies]; member crates use crate.workspace = true. Stops version drift across crates.
interprocess = "2" — semver-compatible range; resolver locks 2.4.x. The tokio feature exists on 2.x.
rustfmt.toml is stable-only — initial draft had imports_granularity and group_imports (nightly-only). Removed to keep cargo fmt --check warning-free.
Pre-declared deps consumed later — notify-rust, dirs, toml, toml_edit, unicode-width, tracing*, serde*, chrono are declared at the workspace root but not pulled into any crate yet. Resolver validates them at lock time; downstream waves consume them. <a data-href="build-log-02a-seed-core-foundation" href="05-notes/build-log-02a-seed-core-foundation.html" class="internal-link" target="_self" rel="noopener nofollow">build-log-02a-seed-core-foundation</a> — Wave 2A built on this scaffold.
]]></description><link>05-notes/build-log-01-workspace-bootstrap.html</link><guid isPermaLink="false">05 notes/build-log-01-workspace-bootstrap.md</guid><pubDate>Wed, 29 Apr 2026 07:33:48 GMT</pubDate></item><item><title><![CDATA[BACKLOG.kanban]]></title><description><![CDATA[Inbox0<a aria-label="More options" class="kanban-plugin__lane-settings-button clickable-icon"><span data-icon="lucide-more-vertical" class="kanban-plugin__icon "><svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="svg-icon lucide-more-vertical"><circle cx="12" cy="12" r="1"></circle><circle cx="12" cy="5" r="1"></circle><circle cx="12" cy="19" r="1"></circle></svg></span></a>+ Add a cardScoping1<a aria-label="More options" class="kanban-plugin__lane-settings-button clickable-icon"><span data-icon="lucide-more-vertical" class="kanban-plugin__icon "><svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="svg-icon lucide-more-vertical"><circle cx="12" cy="12" r="1"></circle><circle cx="12" cy="5" r="1"></circle><circle cx="12" cy="19" r="1"></circle></svg></span></a>TASK-014 · Close the loop without the TUI [D, L]<br>
Decide between Option A (seed log &lt;verb&gt; CLI subcommand) or Option B (rescope README to "open seed to log completions"). See <a data-href="v0-1-0-punch-list#TASK-014 · Close the loop without the TUI [D, L" href="08-specs/v0-1-0-punch-list.html#TASK-014 · Close the loop without the TUI [D, L" class="internal-link" target="_self" rel="noopener nofollow">v0-1-0-punch-list &gt; TASK-014 · Close the loop without the TUI [D, L</a>].<a data-ignore-drag="true" aria-label="More options" class="kanban-plugin__item-postfix-button clickable-icon"><span data-icon="lucide-more-vertical" class="kanban-plugin__icon "><svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="svg-icon lucide-more-vertical"><circle cx="12" cy="12" r="1"></circle><circle cx="12" cy="5" r="1"></circle><circle cx="12" cy="19" r="1"></circle></svg></span></a>+ Add a cardReady14<a aria-label="More options" class="kanban-plugin__lane-settings-button clickable-icon"><span data-icon="lucide-more-vertical" class="kanban-plugin__icon "><svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="svg-icon lucide-more-vertical"><circle cx="12" cy="12" r="1"></circle><circle cx="12" cy="5" r="1"></circle><circle cx="12" cy="19" r="1"></circle></svg></span></a>TASK-015 · Reconcile docs with parser + flags [F]<br>
README/help drift; missing ?&lt;skill&gt;, /random, /all &lt;n&gt;, snooze; --dev gate undocumented. See <a data-href="v0-1-0-punch-list#TASK-015 · Reconcile docs with parser + flags [F" href="08-specs/v0-1-0-punch-list.html#TASK-015 · Reconcile docs with parser + flags [F" class="internal-link" target="_self" rel="noopener nofollow">v0-1-0-punch-list &gt; TASK-015 · Reconcile docs with parser + flags [F</a>].<a data-ignore-drag="true" aria-label="More options" class="kanban-plugin__item-postfix-button clickable-icon"><span data-icon="lucide-more-vertical" class="kanban-plugin__icon "><svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="svg-icon lucide-more-vertical"><circle cx="12" cy="12" r="1"></circle><circle cx="12" cy="5" r="1"></circle><circle cx="12" cy="19" r="1"></circle></svg></span></a>TASK-018 · Restore seed-core purity [H]<br>
Move file I/O out of seed_core::config so core stays pure. Unblocks TASK-016. See <a data-href="v0-1-0-punch-list#TASK-018 · Restore `seed-core` purity [H" href="08-specs/v0-1-0-punch-list.html#TASK-018 · Restore `seed-core` purity [H" class="internal-link" target="_self" rel="noopener nofollow">v0-1-0-punch-list &gt; TASK-018 · Restore `seed-core` purity [H</a>].<a data-ignore-drag="true" aria-label="More options" class="kanban-plugin__item-postfix-button clickable-icon"><span data-icon="lucide-more-vertical" class="kanban-plugin__icon "><svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="svg-icon lucide-more-vertical"><circle cx="12" cy="12" r="1"></circle><circle cx="12" cy="5" r="1"></circle><circle cx="12" cy="19" r="1"></circle></svg></span></a>TASK-016 · CONFIG tab — read-only v0.1.0 cut [F, H]<br>
Add SideTab::Config rendering the resolved Config; full editable surface deferred to v0.2.0. See <a data-href="v0-1-0-punch-list#TASK-016 · CONFIG tab — read-only v0.1.0 cut [F, H" href="08-specs/v0-1-0-punch-list.html#TASK-016 · CONFIG tab — read-only v0.1.0 cut [F, H" class="internal-link" target="_self" rel="noopener nofollow">v0-1-0-punch-list &gt; TASK-016 · CONFIG tab — read-only v0.1.0 cut [F, H</a>].<a data-ignore-drag="true" aria-label="More options" class="kanban-plugin__item-postfix-button clickable-icon"><span data-icon="lucide-more-vertical" class="kanban-plugin__icon "><svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="svg-icon lucide-more-vertical"><circle cx="12" cy="12" r="1"></circle><circle cx="12" cy="5" r="1"></circle><circle cx="12" cy="19" r="1"></circle></svg></span></a>TASK-020 · Single source of truth for the wire protocol [H]<br>
Have seed-tui depend on seed-daemon's lib and use seed_daemon::wire::* instead of duplicating the protocol. See <a data-href="v0-1-0-punch-list#TASK-020 · Single source of truth for the wire protocol [H" href="08-specs/v0-1-0-punch-list.html#TASK-020 · Single source of truth for the wire protocol [H" class="internal-link" target="_self" rel="noopener nofollow">v0-1-0-punch-list &gt; TASK-020 · Single source of truth for the wire protocol [H</a>].<a data-ignore-drag="true" aria-label="More options" class="kanban-plugin__item-postfix-button clickable-icon"><span data-icon="lucide-more-vertical" class="kanban-plugin__icon "><svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="svg-icon lucide-more-vertical"><circle cx="12" cy="12" r="1"></circle><circle cx="12" cy="5" r="1"></circle><circle cx="12" cy="19" r="1"></circle></svg></span></a>TASK-021 · Fix forward-compat asymmetry [H, L]<br>
Replace TUI's hand-rolled deserialize with seed_core::events::from_envelope. Stops the TUI silently dropping unknown future events. Prereq for TASK-024. See <a data-href="v0-1-0-punch-list#TASK-021 · Fix forward-compat asymmetry [H, L" href="08-specs/v0-1-0-punch-list.html#TASK-021 · Fix forward-compat asymmetry [H, L" class="internal-link" target="_self" rel="noopener nofollow">v0-1-0-punch-list &gt; TASK-021 · Fix forward-compat asymmetry [H, L</a>].<a data-ignore-drag="true" aria-label="More options" class="kanban-plugin__item-postfix-button clickable-icon"><span data-icon="lucide-more-vertical" class="kanban-plugin__icon "><svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="svg-icon lucide-more-vertical"><circle cx="12" cy="12" r="1"></circle><circle cx="12" cy="5" r="1"></circle><circle cx="12" cy="19" r="1"></circle></svg></span></a>TASK-022 · Single source of truth for event kinds [H]<br>
Define event kinds once; derive is_known_kind and event_kind from that single source. Pairs with TASK-021. See <a data-href="v0-1-0-punch-list#TASK-022 · Single source of truth for event kinds [H" href="08-specs/v0-1-0-punch-list.html#TASK-022 · Single source of truth for event kinds [H" class="internal-link" target="_self" rel="noopener nofollow">v0-1-0-punch-list &gt; TASK-022 · Single source of truth for event kinds [H</a>].<a data-ignore-drag="true" aria-label="More options" class="kanban-plugin__item-postfix-button clickable-icon"><span data-icon="lucide-more-vertical" class="kanban-plugin__icon "><svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="svg-icon lucide-more-vertical"><circle cx="12" cy="12" r="1"></circle><circle cx="12" cy="5" r="1"></circle><circle cx="12" cy="19" r="1"></circle></svg></span></a>TASK-023 · Schema doc reconciliation [H]<br>
Doc-side fixes shipped (10 tiers, LevelUp.old_level, seed.reminder.interval_changed, prestige status flips). Open: code-side enforcement test that fails when schema and Tier/Event enums drift. Pairs with TASK-022. See <a data-href="v0-1-0-punch-list#TASK-023 · Schema doc reconciliation [H" href="08-specs/v0-1-0-punch-list.html#TASK-023 · Schema doc reconciliation [H" class="internal-link" target="_self" rel="noopener nofollow">v0-1-0-punch-list &gt; TASK-023 · Schema doc reconciliation [H</a>].<a data-ignore-drag="true" aria-label="More options" class="kanban-plugin__item-postfix-button clickable-icon"><span data-icon="lucide-more-vertical" class="kanban-plugin__icon "><svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="svg-icon lucide-more-vertical"><circle cx="12" cy="12" r="1"></circle><circle cx="12" cy="5" r="1"></circle><circle cx="12" cy="19" r="1"></circle></svg></span></a>TASK-025 · Emit TierChanged + tier-up toast [L]<br>
Daemon must emit TierChanged on level-up boundaries; TUI shows distinctive toast. Currently Event::TierChanged exists but has zero producers. See <a data-href="v0-1-0-punch-list#TASK-025 · Emit `TierChanged` + tier-up toast [L" href="08-specs/v0-1-0-punch-list.html#TASK-025 · Emit `TierChanged` + tier-up toast [L" class="internal-link" target="_self" rel="noopener nofollow">v0-1-0-punch-list &gt; TASK-025 · Emit `TierChanged` + tier-up toast [L</a>].<a data-ignore-drag="true" aria-label="More options" class="kanban-plugin__item-postfix-button clickable-icon"><span data-icon="lucide-more-vertical" class="kanban-plugin__icon "><svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="svg-icon lucide-more-vertical"><circle cx="12" cy="12" r="1"></circle><circle cx="12" cy="5" r="1"></circle><circle cx="12" cy="19" r="1"></circle></svg></span></a>TASK-024 · Prestige minimum-viable surface [L]<br>
Token counter on LEVELS tab + "INTEGRATE READY" hint at lvl 99 — or explicit defer. Currently tokens accumulate silently. Depends on TASK-021. See <a data-href="v0-1-0-punch-list#TASK-024 · Prestige minimum-viable surface [L" href="08-specs/v0-1-0-punch-list.html#TASK-024 · Prestige minimum-viable surface [L" class="internal-link" target="_self" rel="noopener nofollow">v0-1-0-punch-list &gt; TASK-024 · Prestige minimum-viable surface [L</a>].<a data-ignore-drag="true" aria-label="More options" class="kanban-plugin__item-postfix-button clickable-icon"><span data-icon="lucide-more-vertical" class="kanban-plugin__icon "><svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="svg-icon lucide-more-vertical"><circle cx="12" cy="12" r="1"></circle><circle cx="12" cy="5" r="1"></circle><circle cx="12" cy="19" r="1"></circle></svg></span></a>TASK-027 · EventLog durability fixes [H]<br>
count_lines UTF-8 drift, non-atomic Windows rename in snapshot_write, dead cfg in append. See <a data-href="v0-1-0-punch-list#TASK-027 · `EventLog` durability fixes [H" href="08-specs/v0-1-0-punch-list.html#TASK-027 · `EventLog` durability fixes [H" class="internal-link" target="_self" rel="noopener nofollow">v0-1-0-punch-list &gt; TASK-027 · `EventLog` durability fixes [H</a>].<a data-ignore-drag="true" aria-label="More options" class="kanban-plugin__item-postfix-button clickable-icon"><span data-icon="lucide-more-vertical" class="kanban-plugin__icon "><svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="svg-icon lucide-more-vertical"><circle cx="12" cy="12" r="1"></circle><circle cx="12" cy="5" r="1"></circle><circle cx="12" cy="19" r="1"></circle></svg></span></a>TASK-019 · Stop TUI from mutating shared State [H]<br>
Introduce App.client_log separate from State.log; LOG view renders both. See <a data-href="v0-1-0-punch-list#TASK-019 · Stop TUI from mutating shared `State` [H" href="08-specs/v0-1-0-punch-list.html#TASK-019 · Stop TUI from mutating shared `State` [H" class="internal-link" target="_self" rel="noopener nofollow">v0-1-0-punch-list &gt; TASK-019 · Stop TUI from mutating shared `State` [H</a>].<a data-ignore-drag="true" aria-label="More options" class="kanban-plugin__item-postfix-button clickable-icon"><span data-icon="lucide-more-vertical" class="kanban-plugin__icon "><svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="svg-icon lucide-more-vertical"><circle cx="12" cy="12" r="1"></circle><circle cx="12" cy="5" r="1"></circle><circle cx="12" cy="19" r="1"></circle></svg></span></a>TASK-026 · Richer overdue indicator [D, L]<br>
Status bar shows OVERDUE: N · SNOOZED: M · COMPLETED: K. Coordinate with TASK-017. See <a data-href="v0-1-0-punch-list#TASK-026 · Richer overdue indicator [D, L" href="08-specs/v0-1-0-punch-list.html#TASK-026 · Richer overdue indicator [D, L" class="internal-link" target="_self" rel="noopener nofollow">v0-1-0-punch-list &gt; TASK-026 · Richer overdue indicator [D, L</a>].<a data-ignore-drag="true" aria-label="More options" class="kanban-plugin__item-postfix-button clickable-icon"><span data-icon="lucide-more-vertical" class="kanban-plugin__icon "><svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="svg-icon lucide-more-vertical"><circle cx="12" cy="12" r="1"></circle><circle cx="12" cy="5" r="1"></circle><circle cx="12" cy="19" r="1"></circle></svg></span></a>Minor / nit batch<br>
Bundled cleanup PR after the larger items land: q-key affordance, eprintln in apply_event, double-pinned crossterm, dead pubs, etc. See <a data-href="v0-1-0-punch-list#Minor / nit cleanup batch" href="08-specs/v0-1-0-punch-list.html#Minor / nit cleanup batch" class="internal-link" target="_self" rel="noopener nofollow">v0-1-0-punch-list &gt; Minor / nit cleanup batch</a>.<a data-ignore-drag="true" aria-label="More options" class="kanban-plugin__item-postfix-button clickable-icon"><span data-icon="lucide-more-vertical" class="kanban-plugin__icon "><svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="svg-icon lucide-more-vertical"><circle cx="12" cy="12" r="1"></circle><circle cx="12" cy="5" r="1"></circle><circle cx="12" cy="19" r="1"></circle></svg></span></a>TASK-028 · Carve app.rs and glyph.rs [H]<br>
Modularise app.rs (1089 lines) and glyph.rs::render_glyph (800 lines). Last, to avoid merge churn. See <a data-href="v0-1-0-punch-list#TASK-028 · Carve `app.rs` and `glyph.rs` [H" href="08-specs/v0-1-0-punch-list.html#TASK-028 · Carve `app.rs` and `glyph.rs` [H" class="internal-link" target="_self" rel="noopener nofollow">v0-1-0-punch-list &gt; TASK-028 · Carve `app.rs` and `glyph.rs` [H</a>].<a data-ignore-drag="true" aria-label="More options" class="kanban-plugin__item-postfix-button clickable-icon"><span data-icon="lucide-more-vertical" class="kanban-plugin__icon "><svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="svg-icon lucide-more-vertical"><circle cx="12" cy="12" r="1"></circle><circle cx="12" cy="5" r="1"></circle><circle cx="12" cy="19" r="1"></circle></svg></span></a>+ Add a cardIn Progress2<a aria-label="More options" class="kanban-plugin__lane-settings-button clickable-icon"><span data-icon="lucide-more-vertical" class="kanban-plugin__icon "><svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="svg-icon lucide-more-vertical"><circle cx="12" cy="12" r="1"></circle><circle cx="12" cy="5" r="1"></circle><circle cx="12" cy="19" r="1"></circle></svg></span></a>TASK-013 · Snooze reachable from TUI [F, D]<br>
Daemon side exists; needs TUI dispatch (s keybinding or snooze &lt;word&gt; verb), orbit card visual indicator, parse test. See <a data-href="v0-1-0-punch-list#TASK-013 · Snooze reachable from TUI [F, D" href="08-specs/v0-1-0-punch-list.html#TASK-013 · Snooze reachable from TUI [F, D" class="internal-link" target="_self" rel="noopener nofollow">v0-1-0-punch-list &gt; TASK-013 · Snooze reachable from TUI [F, D</a>].<a data-ignore-drag="true" aria-label="More options" class="kanban-plugin__item-postfix-button clickable-icon"><span data-icon="lucide-more-vertical" class="kanban-plugin__icon "><svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="svg-icon lucide-more-vertical"><circle cx="12" cy="12" r="1"></circle><circle cx="12" cy="5" r="1"></circle><circle cx="12" cy="19" r="1"></circle></svg></span></a>TASK-017 · Coordinate overdue → reset before tag [D, L]<br>
Data-model + scheduler shipped (was_snoozed flag, traits_skipped aggregation). Pending: confirm LEVELS ▾N indicator + skill-detail "Skipped" line render in TUI. See <a data-href="v0-1-0-punch-list#TASK-017 · Coordinate overdue → reset before tag [D, L" href="08-specs/v0-1-0-punch-list.html#TASK-017 · Coordinate overdue → reset before tag [D, L" class="internal-link" target="_self" rel="noopener nofollow">v0-1-0-punch-list &gt; TASK-017 · Coordinate overdue → reset before tag [D, L</a>].<a data-ignore-drag="true" aria-label="More options" class="kanban-plugin__item-postfix-button clickable-icon"><span data-icon="lucide-more-vertical" class="kanban-plugin__icon "><svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="svg-icon lucide-more-vertical"><circle cx="12" cy="12" r="1"></circle><circle cx="12" cy="5" r="1"></circle><circle cx="12" cy="19" r="1"></circle></svg></span></a>+ Add a cardIn Review0<a aria-label="More options" class="kanban-plugin__lane-settings-button clickable-icon"><span data-icon="lucide-more-vertical" class="kanban-plugin__icon "><svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="svg-icon lucide-more-vertical"><circle cx="12" cy="12" r="1"></circle><circle cx="12" cy="5" r="1"></circle><circle cx="12" cy="19" r="1"></circle></svg></span></a>+ Add a cardDone0<a aria-label="More options" class="kanban-plugin__lane-settings-button clickable-icon"><span data-icon="lucide-more-vertical" class="kanban-plugin__icon "><svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="svg-icon lucide-more-vertical"><circle cx="12" cy="12" r="1"></circle><circle cx="12" cy="5" r="1"></circle><circle cx="12" cy="19" r="1"></circle></svg></span></a>+ Add a card]]></description><link>00-index/backlog.kanban.html</link><guid isPermaLink="false">00 index/BACKLOG.kanban.md</guid><pubDate>Wed, 29 Apr 2026 07:14:00 GMT</pubDate></item><item><title><![CDATA[cargo-cheatsheet]]></title><description><![CDATA[The workspace ships two binaries and one library:There is no serve binary. If you tried cargo run --bin serve (or any
shorthand like cargo run -- serve), use seedd instead.cargo build --workspace # debug build, all crates
cargo build --release --workspace # release build (use for "real" runs)
cargo build -p seed-tui # one crate
cargo check --workspace # typecheck only — fastest feedback loop
Output binaries: target/{debug,release}/seed{,d}{,.exe}.The TUI auto-spawns the daemon if one isn't already running, so you usually
just cargo run --bin seed. Run them separately when you want logs.cargo run --bin seed # launch the TUI
cargo run --bin seed -- init # scaffold ~/.seed/config.toml
cargo run --bin seed -- --help # CLI help cargo run --bin seedd # daemon (logs to ~/.seed/seed.log)
cargo run --bin seedd -- --foreground # daemon, logs to stderr
cargo run --bin seedd -- --version # Override the state directory (useful for sandboxed dev runs):
cargo run --bin seed -- --seed-home /tmp/seed-dev
SEED_HOME=/tmp/seed-dev cargo run --bin seed
Anything after -- is forwarded to the binary; flags before -- go to cargo.SEED_LOG=debug cargo run --bin seedd -- --foreground
RUST_BACKTRACE=1 cargo run --bin seed
cargo test --workspace # all unit + integration tests
cargo test -p seed-core # one crate
cargo test -p seed-tui --tests # only integration tests in tests/
cargo test -p seed-core --lib # only inline unit tests
cargo test golden_snapshot # one test by name (substring match)
cargo test glyph::tests:: # by module path prefix
-- --nocapture lets println! reach stdout (otherwise tests swallow it):cargo test -p seed-core glyph -- --nocapture
Some tests are gated #[ignore] because they spawn real processes or write
golden snapshots. Run them explicitly:cargo test --test smoke -- --ignored # daemon spawn smoke
cargo test -p seed-core --test glyph dump_golden -- --ignored --nocapture
When you intentionally change the renderer:
cargo test -p seed-core --test glyph dump_golden -- --ignored --nocapture
Copy the lines between --- BEGIN SNAPSHOT --- and --- END SNAPSHOT ---
Paste them into crates/seed-core/tests/glyph_golden.txt (no trailing
blank line content — only one trailing newline).
cargo test -p seed-core golden_snapshot must pass.
cargo fmt --all # apply rustfmt
cargo fmt --all -- --check # check only (CI-friendly)
cargo clippy --workspace -- -D warnings
cargo clippy --workspace --all-targets -- -D warnings # also lint test code
cargo fix --workspace --allow-dirty # apply auto-fixes (review the diff)
cargo tree -p seed-tui # dep tree for one crate
cargo tree -d # show duplicate deps across versions
cargo doc --workspace --no-deps --open # generate + open API docs
cargo expand -p seed-core glyph # macro-expanded source (needs cargo-expand)
cargo clean # blow away ./target (will trigger full rebuild)
cargo clean -p seed-tui # one crate's artefacts only
cargo check -p seed-tui # ~1-3s typecheck
cargo test -p seed-tui --tests # run that crate's tests
# Terminal 1 — verbose daemon in foreground
SEED_LOG=debug cargo run --bin seedd -- --foreground # Terminal 2 — TUI talks to that daemon
cargo run --bin seed
If the TUI keeps spawning a second daemon, it means the IPC socket /
named pipe is missing or stale. Stop both, delete ~/.seed/seedd.sock (or
the equivalent named pipe on Windows) and retry. On Windows the pipe lives
under \\.\pipe\ and is cleaned up automatically when the daemon exits.SEED_HOME=/tmp/seed-sandbox cargo run --bin seed -- init
SEED_HOME=/tmp/seed-sandbox cargo run --bin seed
cargo fmt --all -- --check &amp;&amp; \
cargo clippy --workspace --all-targets -- -D warnings &amp;&amp; \
cargo test --workspace error: no bin target named X — only seed and seedd exist; see the
table at the top.
TUI hangs on launch — daemon is unreachable. Run it manually with
--foreground and watch the logs.
Logs are empty — default log level is warn. Set SEED_LOG=info (or
finer) before running.
Tests pass locally, fail in CI on golden snapshot — golden file has a
trailing-whitespace or line-ending issue. Regenerate it via the steps above
and commit only the LF-normalised file.
linker LNK1181 / lld errors on Windows — usually a stale target/
directory after a toolchain upgrade. cargo clean and rebuild.
]]></description><link>03-guides/cargo-cheatsheet.html</link><guid isPermaLink="false">03 guides/cargo-cheatsheet.md</guid><pubDate>Wed, 29 Apr 2026 06:55:15 GMT</pubDate></item><item><title><![CDATA[README]]></title><description><![CDATA[This is an <a data-tooltip-position="top" aria-label="https://obsidian.md" rel="noopener nofollow" class="external-link is-unresolved" href="https://obsidian.md" target="_self">Obsidian</a> vault containing all project documentation. It follows a numbered Zettelkasten-inspired directory structure for consistent knowledge organization.<br>Documentation is published to GitHub Pages via the <a data-tooltip-position="top" aria-label="https://github.com/KosmosisDire/obsidian-webpage-export" rel="noopener nofollow" class="external-link is-unresolved" href="https://github.com/KosmosisDire/obsidian-webpage-export" target="_self">Webpage HTML Export</a> plugin. Export is triggered manually from within Obsidian — this is intentional, providing a human-in-the-loop review step before publication.Exported HTML is written to the docs/ directory at the project root, which GitHub Pages serves.]]></description><link>readme.html</link><guid isPermaLink="false">README.md</guid><pubDate>Thu, 23 Apr 2026 05:38:01 GMT</pubDate></item><item><title><![CDATA[PRS]]></title><description><![CDATA[Views0 resultsSortFilterPropertiesSearchNewUnable to parse your base file:e.startsWith is not a function]]></description><link>00-index/prs.html</link><guid isPermaLink="false">00 index/PRS.base</guid><pubDate>Tue, 07 Apr 2026 03:02:56 GMT</pubDate></item><item><title><![CDATA[SPECS]]></title><description><![CDATA[All Specs7 resultsSort0Filter0PropertiesSearchNewShowing 7file nameglyph-expansionoverdue-rolloverprestige-focusprestige-integrateSPECSv0-1-0-punch-listxp-pacing]]></description><link>00-index/specs.html</link><guid isPermaLink="false">00 index/SPECS.base</guid><pubDate>Tue, 07 Apr 2026 03:02:55 GMT</pubDate></item><item><title><![CDATA[NOTES]]></title><description><![CDATA[Recent First9 resultsSort0Filter0PropertiesSearchNewShowing 9date modifiedWednesday, April 29th 2026, 7:53:27 amWednesday, April 29th 2026, 7:53:23 amWednesday, April 29th 2026, 7:53:23 amWednesday, April 29th 2026, 7:53:23 amWednesday, April 29th 2026, 7:53:23 amWednesday, April 29th 2026, 7:53:22 amWednesday, April 29th 2026, 7:53:22 amWednesday, April 29th 2026, 7:53:22 amWednesday, April 29th 2026, 7:53:26 am]]></description><link>00-index/notes.html</link><guid isPermaLink="false">00 index/NOTES.base</guid><pubDate>Tue, 07 Apr 2026 03:02:54 GMT</pubDate></item><item><title><![CDATA[GUIDES]]></title><description><![CDATA[All Guides2 resultsSort0Filter0PropertiesSearchNewShowing 2file namecargo-cheatsheetGUIDES]]></description><link>00-index/guides.html</link><guid isPermaLink="false">00 index/GUIDES.base</guid><pubDate>Tue, 07 Apr 2026 03:02:51 GMT</pubDate></item><item><title><![CDATA[CONCEPTS]]></title><description><![CDATA[All Concepts1 resultSort0Filter0PropertiesSearchNewShowing 1file nameCONCEPTS]]></description><link>00-index/concepts.html</link><guid isPermaLink="false">00 index/CONCEPTS.base</guid><pubDate>Tue, 07 Apr 2026 03:02:49 GMT</pubDate></item><item><title><![CDATA[ADR]]></title><description><![CDATA[All ADRs1 resultSort0Filter0PropertiesSearchNewShowing 1file nameADR]]></description><link>00-index/adr.html</link><guid isPermaLink="false">00 index/ADR.base</guid><pubDate>Tue, 07 Apr 2026 03:02:46 GMT</pubDate></item><item><title><![CDATA[REFERENCES]]></title><description><![CDATA[All References3 resultsSort0Filter0PropertiesSearchNewShowing 3file namecli-flagsevents-schemaREFERENCES]]></description><link>00-index/references.html</link><guid isPermaLink="false">00 index/REFERENCES.base</guid><pubDate>Tue, 07 Apr 2026 00:19:34 GMT</pubDate></item></channel></rss>